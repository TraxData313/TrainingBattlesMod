- [x] Feasibility research — the whole V1 verified against the real game DLLs, verdict: ACHIEVABLE, no blockers. Decompiled this game version's TaleWorlds.CampaignSystem, SandBox, and TaleWorlds.MountAndBlade into `..\reference\game-decompiled\` (ilspycmd with `$env:DOTNET_ROLL_FORWARD='LatestMajor'`; note SandBox.dll lives under `Modules\SandBox\bin`, not the main `bin`) and read the actual code for every V1 piece. The finds: (1) the lair-style troop-picker GUI is a ready-made public API — `PartyScreenHelper.OpenScreenWithDummyRosterWithMainParty` with heroes transferable and a built-in Cancel; (2) the mock battle has an EXACT vanilla precedent — the Company of Trouble quest builds a temp party from the player's own troops (bandit party component for hostility, `SetPartyUsedByQuest` to shield it from world AI), forces the fight via `PlayerEncounter.Start/SetupFields/StartBattle` + `CampaignMission.OpenBattleMission` on the local map-patch scene, reads `Battle.WinningSide`, `Finish()`es (no loot screen), destroys the temp party — our whole battle loop is a copy of shipped vanilla code; (3) aftermath is all public APIs — roster snapshot/diff via `CloneRosterData` + `AddToCounts(..., wounded, xp)`, dead→wounded through the game's own surgeon math `PartyHealingModel.GetSurvivalChance` (Medicine-driven, so "a good doctor helps" falls out of the vanilla model) + `SkillLevelingManager.OnSurgeryApplied` (the doctor levels from training too), `MobileParty.SetDisorganized(true)` for the disorganized state, and a `BattleRewardModel` campaign-model override zeroing renown/influence during training (no Harmony anywhere in the core loop); (4) the terrain-picker dream is surprisingly cheap — vanilla keeps `GameSceneDataManager.SingleplayerBattleScenes` (SceneID/Terrain/IsNaval/MapIndices per scene), picks RANDOMLY when several match a map patch, so a custom `SceneModel` override can enumerate the candidates, let the player choose, and the same override later serves picking your ground when DEFENDING real battles + naval (the `isNavalEncounter` flag rides the same API); (5) MCM integration copies the proven `McmBridge` pattern from ImmersiveAI. Ranked playtest risks (research doc §6): companion heroes riding the temp Team-2 party, the player-defeat path must never reach capture, XP-on-roster confirmation, loot/prisoner residue, real-Army parties out of V1 scope. Prior-art search: nothing does this (closest are arena-only Troop Training Expanded and battle-less SplitParty) — the niche is open. Full write-up with code snippets: docs/training-battle-research.md. (2026.07.23 08.43.57)
- [x] V1 CORE BUILT — the whole training-battle loop, in one pass, straight from the research doc. NOT YET PLAYTESTED. The solution scaffold mirrors ImmersiveAI exactly: `src/TrainingBattles.Core` (netstandard2.0, pure logic), `src/TrainingBattles.Module` (net472, the Bannerlord module), `tests/TrainingBattles.Core.Tests` (net8.0 xUnit, 25 tests green), `module/SubModule.xml` (optional MCM dependency declared), `tools/deploy.ps1` (installs as "Training Battles (dev)", module id TrainingBattles.Dev). Core: `AftermathMath` (per-fallen two-roll pipeline — the surgeon's save first, then the wounded-share roll, nobody dies; XP kept/removed split with away-from-zero rounding and a kept+removed==earned invariant test) and `TrainingCooldown` (0 = unlimited, never-trained = ready). Module: `ModConfig` (config.json under Configs\TrainingBattles, clamped Normalize, single source of truth), `Mcm\` (the ImmersiveAI McmBridge pattern verbatim — soft dependency, menu is a live editor over config.json; 5 settings: XP kept %, wounded %, disorganized toggle, cooldown hours, hotkey), `Models\TrainingBattleRewardModel` (the "it was only training" guard: subclasses DefaultBattleRewardModel and zeroes renown/influence/morale/relation/gold/item loot/banner loot AND forbids prisoner-taking + member capture while `TrainingBattleBehavior.TrainingActive`), and `TrainingBattleBehavior` — the heart: hotkey (default T, MCM-configurable) opens the muster menu from the open map (blocked with honest reasons in settlement/army/siege/engagement); "Divide the men" opens `PartyScreenHelper.OpenScreenWithDummyRoster` over a CLONE of the member roster (nothing leaves the real party until the battle truly begins — a quit mid-pick can never lose a man; player not transferable, companions are); Begin (attack or defend — the SetupFields order decides) moves the picked men clamped-against-live-roster into a temp `BanditPartyComponent` party (`SetPartyUsedByQuest`, custom name "Training Opponents"), snapshots both rosters, raises `TrainingActive`, and runs the exact Company-of-Trouble recipe (PlayerEncounter.Start/SetupFields/StartBattle + OpenBattleMission on the local map patch scene); the menu's re-init after the mission runs the aftermath — winner read from `Battle.WinningSide`, `PlayerEncounter.Finish()`, survivors + any prisoners merged home, the fallen restored via before/after roster diff with `AftermathMath` (surgeon save = the game's own `PartyHealingModel.GetSurvivalChance`, surgeon credited via `OnSurgeryApplied` per save), XP delta scaled to the configured percent, `SetDisorganized(true)`, cooldown stamped, one honest summary line, menu exits. Safety nets: cancel = dropping the pick (real rosters untouched); a failed battle start unwinds fully without burning the cooldown; `RecoverStaleOpponentParties` at session launch merges home and dissolves any `training_opponents_*` party a crash left in a save. Build clean against the real game DLLs + MCMv5 on the FIRST compile (the decompiled-corpus research paid for itself), deployed to Modules\TrainingBattles.Dev. Next: Anton's first playtest against the research doc §6 risk list. (2026.07.23 09.02.54)
- [x] BUGFIX round 1 from Anton's first playtest (divide worked; attack/defend/cancel dead; pick forgotten). Three fixes: (1) Cancel training now actually leaves the menu - isLeave only styles a GameMenuOption, the exit is on the consequence, so CancelTraining() calls GameMenu.ExitToLast(). (2) The picker close callback fires MID state-transition (the party screen is still popping); re-entering the muster menu from inside it was fragile - PickerClosed now only records the pick and raises _returnToMenuPending, and the next application tick (once the MapState is truly back) does SwitchToMenu/ActivateGameMenu. It also shows an honest side message on close ("opposing half set - N able men" / "selection closed without changes") so a lost pick is visible immediately instead of silently arming nothing. (3) The pick is REMEMBERED: reopening the picker pre-loads the previous selection on the left, subtracted (clamped against the live roster) from the right side, instead of starting empty. Belt-and-braces alongside: PickerDoneCondition wrapped so a throwing condition can never lock the Done button shut, and BeginBattle wrapped whole so any failure surfaces as a message + full unwind instead of a silent nothing. 25 tests green, deployed. (2026.07.23 10.06.50)
