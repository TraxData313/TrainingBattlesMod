- [x] Feasibility research â€” the whole V1 verified against the real game DLLs, verdict: ACHIEVABLE, no blockers. Decompiled this game version's TaleWorlds.CampaignSystem, SandBox, and TaleWorlds.MountAndBlade into `..\reference\game-decompiled\` (ilspycmd with `$env:DOTNET_ROLL_FORWARD='LatestMajor'`; note SandBox.dll lives under `Modules\SandBox\bin`, not the main `bin`) and read the actual code for every V1 piece. The finds: (1) the lair-style troop-picker GUI is a ready-made public API â€” `PartyScreenHelper.OpenScreenWithDummyRosterWithMainParty` with heroes transferable and a built-in Cancel; (2) the mock battle has an EXACT vanilla precedent â€” the Company of Trouble quest builds a temp party from the player's own troops (bandit party component for hostility, `SetPartyUsedByQuest` to shield it from world AI), forces the fight via `PlayerEncounter.Start/SetupFields/StartBattle` + `CampaignMission.OpenBattleMission` on the local map-patch scene, reads `Battle.WinningSide`, `Finish()`es (no loot screen), destroys the temp party â€” our whole battle loop is a copy of shipped vanilla code; (3) aftermath is all public APIs â€” roster snapshot/diff via `CloneRosterData` + `AddToCounts(..., wounded, xp)`, deadâ†’wounded through the game's own surgeon math `PartyHealingModel.GetSurvivalChance` (Medicine-driven, so "a good doctor helps" falls out of the vanilla model) + `SkillLevelingManager.OnSurgeryApplied` (the doctor levels from training too), `MobileParty.SetDisorganized(true)` for the disorganized state, and a `BattleRewardModel` campaign-model override zeroing renown/influence during training (no Harmony anywhere in the core loop); (4) the terrain-picker dream is surprisingly cheap â€” vanilla keeps `GameSceneDataManager.SingleplayerBattleScenes` (SceneID/Terrain/IsNaval/MapIndices per scene), picks RANDOMLY when several match a map patch, so a custom `SceneModel` override can enumerate the candidates, let the player choose, and the same override later serves picking your ground when DEFENDING real battles + naval (the `isNavalEncounter` flag rides the same API); (5) MCM integration copies the proven `McmBridge` pattern from ImmersiveAI. Ranked playtest risks (research doc Â§6): companion heroes riding the temp Team-2 party, the player-defeat path must never reach capture, XP-on-roster confirmation, loot/prisoner residue, real-Army parties out of V1 scope. Prior-art search: nothing does this (closest are arena-only Troop Training Expanded and battle-less SplitParty) â€” the niche is open. Full write-up with code snippets: docs/training-battle-research.md. (2026.07.23 08.43.57)
- [x] V1 CORE BUILT â€” the whole training-battle loop, in one pass, straight from the research doc. NOT YET PLAYTESTED. The solution scaffold mirrors ImmersiveAI exactly: `src/TrainingBattles.Core` (netstandard2.0, pure logic), `src/TrainingBattles.Module` (net472, the Bannerlord module), `tests/TrainingBattles.Core.Tests` (net8.0 xUnit, 25 tests green), `module/SubModule.xml` (optional MCM dependency declared), `tools/deploy.ps1` (installs as "Training Battles (dev)", module id TrainingBattles.Dev). Core: `AftermathMath` (per-fallen two-roll pipeline â€” the surgeon's save first, then the wounded-share roll, nobody dies; XP kept/removed split with away-from-zero rounding and a kept+removed==earned invariant test) and `TrainingCooldown` (0 = unlimited, never-trained = ready). Module: `ModConfig` (config.json under Configs\TrainingBattles, clamped Normalize, single source of truth), `Mcm\` (the ImmersiveAI McmBridge pattern verbatim â€” soft dependency, menu is a live editor over config.json; 5 settings: XP kept %, wounded %, disorganized toggle, cooldown hours, hotkey), `Models\TrainingBattleRewardModel` (the "it was only training" guard: subclasses DefaultBattleRewardModel and zeroes renown/influence/morale/relation/gold/item loot/banner loot AND forbids prisoner-taking + member capture while `TrainingBattleBehavior.TrainingActive`), and `TrainingBattleBehavior` â€” the heart: hotkey (default T, MCM-configurable) opens the muster menu from the open map (blocked with honest reasons in settlement/army/siege/engagement); "Divide the men" opens `PartyScreenHelper.OpenScreenWithDummyRoster` over a CLONE of the member roster (nothing leaves the real party until the battle truly begins â€” a quit mid-pick can never lose a man; player not transferable, companions are); Begin (attack or defend â€” the SetupFields order decides) moves the picked men clamped-against-live-roster into a temp `BanditPartyComponent` party (`SetPartyUsedByQuest`, custom name "Training Opponents"), snapshots both rosters, raises `TrainingActive`, and runs the exact Company-of-Trouble recipe (PlayerEncounter.Start/SetupFields/StartBattle + OpenBattleMission on the local map patch scene); the menu's re-init after the mission runs the aftermath â€” winner read from `Battle.WinningSide`, `PlayerEncounter.Finish()`, survivors + any prisoners merged home, the fallen restored via before/after roster diff with `AftermathMath` (surgeon save = the game's own `PartyHealingModel.GetSurvivalChance`, surgeon credited via `OnSurgeryApplied` per save), XP delta scaled to the configured percent, `SetDisorganized(true)`, cooldown stamped, one honest summary line, menu exits. Safety nets: cancel = dropping the pick (real rosters untouched); a failed battle start unwinds fully without burning the cooldown; `RecoverStaleOpponentParties` at session launch merges home and dissolves any `training_opponents_*` party a crash left in a save. Build clean against the real game DLLs + MCMv5 on the FIRST compile (the decompiled-corpus research paid for itself), deployed to Modules\TrainingBattles.Dev. Next: Anton's first playtest against the research doc Â§6 risk list. (2026.07.23 09.02.54)
- [x] BUGFIX round 1 from Anton's first playtest (divide worked; attack/defend/cancel dead; pick forgotten). Three fixes: (1) Cancel training now actually leaves the menu - isLeave only styles a GameMenuOption, the exit is on the consequence, so CancelTraining() calls GameMenu.ExitToLast(). (2) The picker close callback fires MID state-transition (the party screen is still popping); re-entering the muster menu from inside it was fragile - PickerClosed now only records the pick and raises _returnToMenuPending, and the next application tick (once the MapState is truly back) does SwitchToMenu/ActivateGameMenu. It also shows an honest side message on close ("opposing half set - N able men" / "selection closed without changes") so a lost pick is visible immediately instead of silently arming nothing. (3) The pick is REMEMBERED: reopening the picker pre-loads the previous selection on the left, subtracted (clamped against the live roster) from the right side, instead of starting empty. Belt-and-braces alongside: PickerDoneCondition wrapped so a throwing condition can never lock the Done button shut, and BeginBattle wrapped whole so any failure surfaces as a message + full unwind instead of a silent nothing. 25 tests green, deployed. (2026.07.23 10.06.50)
- [x] Playtest round 2 build - one real BUG squashed, two papercuts, one NEW FEATURE. (1) THE UPGRADE-XP BUG (Anton: 15 battle sisters, 8 waiting upgrades melting to 6 after a drill; other troops losing waiting upgrades too). Root cause found in the game's own code, not ours: PartyBase.OnXpChanged CLAMPS every troop stack's XP to (men in stack x max upgrade cost) on every XP write - so when men die in battle the stack shrinks and the next XP write silently destroys the dead men's stored upgrade progress. Our aftermath restored the MEN but scaled XP by delta arithmetic, which cannot see clamped-away XP. Fix, two-pronged: at SPLIT time the men now carry their proportional share of stack XP across to the opponent party (vanilla party-screen behavior; also keeps both halves off their clamp ceilings so battle earnings are not eaten), and the AFTERMATH now restores XP ABSOLUTELY - men come back first (raising the clamp ceiling to full), then each stack's XP is SET to its pre-battle pool plus the kept share of visibly-earned XP (target = beforeXp + XpKept(max(0, after-before))). Conservative by construction: clamp losses mid-battle count as unearned, never as punishment. (2) Default hotkey T -> G (T is the vanilla combat log key); ConfigVersion bumped to 2 with a migration that moves configs still carrying the old T default to G while honoring hand-picked keys; MCM dropdown reordered, ParseKey fallback updated. (3) MCM hint texts shortened to one line each - the long ones were clipped off-screen. (4) NEW: "Send the men in - watch it resolve from the hill" menu option - vanilla's own send-troops road (MenuHelper.EncounterOrderAttack recipe: GameMenu.ExitToLast + PlayerEncounter.InitSimulation(null,null) + MapState.StartBattleSimulation), giving the live casualty-ticker simulation view with Break In for free. Since that road leaves our menu, the aftermath needed a second trigger: a CampaignEvents.MapEventEnded listener (guarded to our opponent party) records the winner and raises _aftermathReady, and the tick re-activates the muster menu once no menu is up and no mission runs - the menu init then runs the same aftermath as the fought path. Winner read from the event as fallback when PlayerEncounter.Battle is already gone. Deferred with research notes in TASKS_TODO: commander pick for the opposing half (CustomPartyComponent carries a settable leader BUT leader-change flips party ownership -> MapFaction risk; needs its own careful session), the halves-as-map-parties redesign (pairs with it), try-to-escape (parked - Cancel is the honest escape until the opponent can chase). 25 tests green, deployed. (2026.07.23 10.39.12)
- [x] Playtest round 3 build - the aftermath stops trusting vanilla's wrap-up (Anton's chaotic-and-golden feedback: sim path still lost upgrades, a vanilla "capture opponent/leave" menu appeared after the hill-watch, retreat led to a re-attack screen + map character swinging at air, defeat flashed captured-then-released). Root understanding: vanilla's encounter state machine OWNS every non-happy path (auto-resolve wrap, retreat, defeat), and our aftermath politely waited for menus vanilla controls - so it ran late (after the capture wrap mutated rosters) or looked lost. Fixes: (1) tick-driven finalization - the aftermath now runs the moment it is decided, cutting vanilla off: trigger (a) our MapEventEnded fired and no mission runs -> FinishTrainingBattle immediately (its PlayerEncounter.Finish tears down whatever wrap menu vanilla pushed, including the capture screen); trigger (b) the player RETREATED out of the mission (encounter alive, no simulation, at a menu that is not ours) -> a drill you walk away from is over: finalize with casualties so far. (2) GetMainPartyMemberScatterChance -> 0 while training (on a real defeat some of the player's men scatter and desert - a silent troop-loss channel our defeat playtest exposed). (3) Prisoner walk-back with honest accounting: vanilla makes the losers' wounded the winner's prisoners; the model forbids it during training, but belt-and-braces the aftermath now snapshots the prison roster at launch and walks back only the DELTA of own men that appeared there (real pre-existing prisoners of the same troop type stay prisoners), before the fallen-restore diff so nobody gets double-restored. (4) Observability: the summary line now prints "Drill XP kept: N (and M upgrade XP restored to the stacks)" so the XP fix is verifiable at a glance. 25 tests green. Deploy pending - the game locks the DLL while Anton plays; a background watcher retries every 30s and installs the moment the game closes. (2026.07.23 10.54.09)
- [x] Playtest round 4 build - the zombie-encounter kill (Anton: canceled an auto-resolve twice, left, sisters dropped 8 -> 6 waiting upgrades AGAIN). The sequence decoded: canceling the first auto-resolve landed on vanilla's encounter menu, our finalizer ran (restored everything, closed shop, disarmed TrainingActive and the reward-model guards) - but left that vanilla menu ALIVE and re-startable. The second auto-resolve and the final leave then ran as a PURE VANILLA battle with every protection closed (vanilla's leave consequence even runs one real simulated combat round on the way out, MenuHelper line ~611) - real casualties, real clamp-destroyed upgrade XP, no restore. Fixes: (1) FinishTrainingBattle now kills the encounter POSITIVELY - if the map event is still undecided it gets SetOverrideWinner(player side), then PlayerEncounter.Finish, then a bounded pop (max 3) of whatever wrap/encounter menus linger, so nothing re-startable survives; (2) the tick finalizer no longer requires BattleSimulation == null - ANY menu that is not ours, while a training is in flight with no mission running, finalizes the drill (covers cancel-of-simulation, not just mission retreat). The empty-swing map animation remains a known cosmetic (opponent party spawns at the player's own position). 25 tests green; deploy via the 30s watcher. (2026.07.23 11.03.43)
- [x] THE REAL XP THIEF FOUND AND SHOT - it was us all along, armed by a TaleWorlds footgun. Anton's third round (8 waiting upgrades -> 6 on BOTH the fought and the hill path) finally exposed it: 8x0.75=6 is EXACTLY XpKeptPercent applied to the WHOLE pool - and the decompiled source confirms why: TroopRoster.CloneRosterData() copies Number and WoundedNumber but silently DROPS each stack's Xp. Every snapshot we ever took read as zero XP, so the aftermath computed earned = (entire pool - 0), kept 75% of it, and taxed a quarter of the party's stored upgrade progress on every single drill - from the very first build. (The XP-clamp discovery from round 2 is real and the absolute-restore design stays, but the clamp was the accomplice, not the thief.) Fix: a CloneWithXp helper (dummy roster + AddToCounts carrying el.Xp; dummies have no OwnerParty so nothing clamps the snapshot itself) now takes all three snapshots (main, opponent, prison). With truthful snapshots the existing absolute-restore math does exactly what it always claimed: target = true pre-battle pool + kept share of true drill earnings. Also from this round's playtest: retreat/cancel no longer resurrects the battle (round-4 kill confirmed working by Anton). LESSON for future sessions, now also in CLAUDE.md: NEVER use CloneRosterData when XP matters. 25 tests green; deploy via watcher. (2026.07.23 11.17.15)
- [x] The earned-XP leak - the finalize was skipping vanilla's ledger (Anton at 100% kept: promotions visible on the battle scoreboard, roster still 8 waiting upgrades, not 9). Root cause: for PLAYER battles the XP/casualty commit does NOT happen at battle end - it runs inside PlayerEncounter's own update state machine (DoApplyMapEventResults -> CalculateAndCommitMapEventResults, both internal, guarded to run once). Our round-4 kill (SetOverrideWinner + immediate Finish) cut the encounter down BEFORE that state ever ran, so everything the men earned in the mission was silently discarded; the mission scoreboard (its own tally) still showed the promotions, which is exactly the ghost Anton saw. Fix in FinishTrainingBattle: give the event a winner only if it lacks one (HasWinner check - never stomp a real result), then drive PlayerEncounter.Update() up to 3 bounded times so vanilla itself settles the books through its guarded once-only commit, THEN Finish + bounded menu pops + merge/restore as before. No reflection, no Harmony - vanilla runs its own bookkeeping, we just refuse to let it be skipped. Also per Anton's call: XpKeptPercent default is 100 for the testing period (ModConfig + MCM; his config already carries 100 - revisit 75 vs 100 before a public release). 25 tests green; deploy via watcher. (2026.07.23 11.53.04)
- [x] The wounded knob finally bites (Anton: XP now super; nudged WoundedPercent to 10, took 14 KIA, got 14 wounded twice in a row). Diagnosis: the game's OWN surgeon converts most mission KIA into roster-WOUNDED before our aftermath ever runs (high Medicine = high conversion) - our filter only processed the truly-removed dead (near zero with a good doctor), and battle-wounded were deliberately left as-is, so vanilla's wounded sailed straight past the knob. Fix: the aftermath now runs EVERY drill casualty through the same two-roll pipeline - the truly dead AND the newly battle-wounded (delta vs the wounded snapshot; pre-battle wounded stay wounded): surgeon save (game survival chance) -> unhurt, else WoundedPercent -> wounded, else shrug it off. Men are restored first, then one wounded-count adjustment sets the stack to pre-battle-wounded + filtered-wounded (clamped to the stack) - negative adjustment HEALS the vanilla-wounded the knob spares. Summary reworded: 'N men fell or were hurt - M wake up wounded, the rest shrug it off.' Surgeon credited per casualty. Also per Anton: WoundedPercent default now 10 (ModConfig + MCM). 25 tests green; deploy via watcher. (2026.07.23 12.02.48)
- [x] Drill report instrumentation (Anton: 22 KIA -> 7 wounded at the 10% knob; under the intended math that outcome is sub-1%-likely, so something adds wounded outside the filter or the visible count is muddied by pre-existing wounded). Instead of another round of deduction, every drill now writes its full aftermath arithmetic to Configs\TrainingBattles\last_drill_report.txt: per stack - snapshot N/W/xp, post-battle N/W/xp, fallen, newWounded, casualties, the surgeon save chance used, woundedFinal from the filter, the wounded adjustment applied, the xp adjustment applied - plus a totals line and the config percents in force. One drill + that file = the exact source of any wrong number on the party screen. Claude can read the file directly from disk next session. 25 tests green; deploy via watcher. (2026.07.23 12.09.03)
- [x] AFTERMATH VERIFIED END-TO-END - Anton's two confirmation drills: 3 and 4 wounded from big casualty counts at the 10% knob, XP declared 'super'. The drill report proves the whole pipeline in one page (42 casualties -> 3 wake wounded, expected ~2.7 at his surgeon's 0.29-0.38 save chances; negative woundedAdjust columns show the filter actively HEALING the vanilla-wounded the knob spares; and the hardest case - two stacks wiped to 0/0/0, element and XP deleted by the game - shows +7906 and +7418 XP restored by the CloneWithXp snapshots + absolute restore). The V1 core loop (divide -> fight/hill-watch -> nobody dies, wounded filtered, XP banked, disorganized, cooldown) is now playtested and working. Remaining before wider release: companion-on-opposing-half check, a deliberate defeat on the current build, and the shipping-defaults decision (XpKept 100 vs 75, Wounded 10 vs more, surgeon-save stacking). (2026.07.23 12.16.26)
- [x] THE GROUND IS YOURS TO CHOOSE â€” both terrain-picker features shipped in one stroke, plus the shipping defaults locked. The mechanism (research doc Â§4, all verified against the decompiled DLLs): vanilla's DefaultSceneModel picks RANDOMLY when several battle scenes claim the map patch, and the candidate pool is enumerable from GameSceneDataManager.SingleplayerBattleScenes. Three new pieces: (1) `BattleSceneCatalog` â€” walks the same candidate chain vanilla walks (exact map-patch match by sceneIndex, else local-terrain fallback), so what we offer is EXACTLY what fate would have rolled between; also owns the shared MultiSelectionInquiry picker (one "As fate wills (random)" entry + one per variant, labeled Terrain + forest density + SceneID, current choice marked). (2) `TrainingBattlesSceneModel : SceneModel` â€” the KEY mod-compat find: `CampaignGameStarter.AddModel<T>(MBGameModel<T>)` hands the newly-added model the PREVIOUSLY-registered one via `Initialize` â†’ `BaseModel`, so the override is a true DECORATOR over vanilla's model or any other mod's, not a replacement; it returns the static one-shot `PendingSceneId` when set (consumed on read) and delegates everything else down the chain. (3) `DefendGroundBehavior` â€” adds a "Survey the ground" option to vanilla's "encounter" menu (index 4, right before "Fight!"): shown only when the player DEFENDS a real FIELD battle (IsFieldBattle + PlayerSide==Defender; attackers keep vanilla â€” the attacker comes to the defender's ground), â‰¥2 candidates exist, and the ChooseGroundWhenDefending toggle (new config + MCM bool, default ON) allows; the pick arms PendingSceneId, which vanilla's own MenuHelper.EncounterAttackConsequence consumes via SceneModel when the mission opens. Leak-proofing is triple: consume-on-read, cleared on ANY player map-event end (fought, auto-resolved, walked away), and cleared by the training abort/finish paths. The TRAINING door: a "Survey the ground â€” choose the battlefield" muster-menu option (hidden when nothing to choose, disabled-with-tooltip when only one scene fits) stores the choice per-drill; launch validates it still fits the CURRENT position (the player may have ridden off since picking), then arms PendingSceneId â€” which our own OpenBattleMission scene query consumes, and, as a free bonus, a Break In from the send-troops simulation lands on the chosen ground too. SHIPPING DEFAULTS locked per Anton's call: XpKeptPercent 100â†’75 (ModConfig + MCM defaults; ConfigVersion 3 migration moves configs still on the testing-period 100 to 75, honoring any other hand-picked value â€” same precedent as the Tâ†’G hotkey migration), WoundedPercent stays 10, surgeon save keeps stacking with the knob as-is. Build clean, 25 tests green. NOT YET PLAYTESTED â€” see TESTS in TODO (chosen scene actually opening, defender-menu option appearing, no leak into later battles). (2026.07.23 12.29.15)
- [x] THE ONE-MAP MYSTERY SOLVED + THE SCOUTING RIDE BUILT (Anton's first survey playtest: "only one battlefield fits this ground: battle_terrain_a" EVERYWHERE). Diagnosis from the game's own data, not our bug: SandBox\ModuleData\sp_battle_scenes.xml (the file GameSceneDataManager loads) claims each map patch index with AT MOST ONE land scene â€” zero duplicate indices across all 76 entries â€” so vanilla's "multiple matches â†’ pick randomly" branch is LEGACY DEAD CODE in this game version (TaleWorlds even guards it with a Debug.FailedAssert calling multiple matches an anomaly). battle_terrain_a (indices 2,136,118) simply owns the whole region Anton rode through; the research doc Â§4 premise (variants per patch) is true in the API but false in the shipped data. CONSEQUENCES SHIPPED: (1) the training "Survey the ground" now offers the WIDER pool â€” the patch's own scene first (marked "this ground"), then every other scene of the local terrain type (BattleSceneCatalog.WiderPoolAt; drills are practice, so drilling on any same-country battlefield is a feature, not a cheat) â€” the picker lives again; (2) real-defense stays STRICT patch-only (fairness), which with vanilla data means it effectively never shows â€” flagged as an explicit DECIDE-for-Anton in TODO (widen it or leave it dormant for modded scene packs); (3) NEW: THE SCOUTING RIDE (Anton's ask: "if there is only one map, let me scout it") â€” muster-menu option "Ride out and scout a battlefield": pick any battlefield from the wider pool and enter it ALONE â€” no battle, no enemy, no encounter, no cooldown, no cost â€” ride the ground, leave with Tab or the escape menu, land back in the muster menu. Recipe (ScoutMission.cs): MissionState.OpenNew under the mission NAME "Camp" so vanilla's campaign walk-around VIEW set attaches (decompiled SandBox.View: camera/HUD/escape-menu/photo-mode, no battle or conversation dependencies â€” the tempting "SimpleMountedPlayer" dev mission registers an EMPTY view list, unusable), over OUR OWN behavior list (MissionOptions + CampaignMissionComponent [verified: every PlayerEncounter/MapEvent touch is null-guarded] + BasicLeaveMissionLogic + border/boundary trio + EquipmentControllerLeaveLogic + ScoutMissionLogic); the player spawns from the scene's first spawn_path_XX (MBSceneUtilities.GetAllSpawnPaths â†’ Path.GetFrameForDistance(0), z snapped to terrain; boundary-centroid and scene-limits-center fallbacks), real battle equipment incl. horse, Controller=Player, SimpleAgentOrigin; Leave key ends via MissionEnded (the vanilla dev-controller shape). Module now references SandBox.dll (Modules\SandBox\bin, already a declared module dependency). Deferred to TODO: scouting WITH companions (needs follow-AI plumbing). Build clean first compile, 25 tests green, deployed. NOT YET PLAYTESTED â€” spawn placement and the mission-name-borrowed view set are the two watch points. (2026.07.23 12.53.54)
- [x] THE TWO PILLARS MADE VISIBLE + THE DEPLOYMENT PREVIEW (Anton: "those are the two main things I want this mod to be" â€” training battles, and scouting/choosing your ground like a real general; the menu should SAY the numbers; scouting should show the DEPLOY so a bad line can disqualify a good map; be honest about whether the deploy is the same). THE DEPLOYMENT FIND (decompiled BattleSpawnPathSelector.FindBestInitialPath): deployment has TWO regimes â€” with map-patch data in the MissionInitializerRecord (what vanilla REAL field battles pass via MenuHelper) the spawn path, pivot and both sides' ends are computed DETERMINISTICALLY from the encounter position + approach direction; WITHOUT patch data (what our training battles passed via the OpenBattleMission string overload, and what the first scout build passed) it picks a RANDOM path and a RANDOM pivot (0.37â€“0.63 of path length) every battle. SHIPPED: (1) ScoutMission.CreatePatchAwareRecord â€” one shared record recipe (patch coordinates of the player's spot + a FIXED assumed approach direction Vec2(0,1), plus the full vanilla field-battle record fields) now used by BOTH the training battle (switched to the record overload of CampaignMission.OpenBattleMission) and the scout ride â†’ the scouted lines ARE the drilled lines, by construction; (2) the scout now runs the game's own BattleSpawnPathSelector on mission start (public class, side-effect-free to instantiate) and spawns the player ON the defender's deployment line facing the attacker's, with a message calling the enemy line's distance in paces and the honest caveat: a drill forms exactly these lines, a REAL defence keeps the ground but the enemy's true approach picks their end (that caveat is in the muster tooltip, the scout picker text and the in-mission message â€” Anton asked to see it in the descriptions); (3) the muster menu is now the mod's front door: two-pillar text (DRILL / SCOUT) with the LIVE config numbers â€” XP kept %, wounded-among-fallen % with "a better surgeon saves more of them first", disorganized, cooldown â€” using {newline} tokens (vanilla's own menu-text line-break mechanism), and the cooldown-wait text now points out scouting needs no rest; the divide-option tooltip repeats the numbers; (4) README rewritten around the two pillars (drill rules and honesty about the deploy promise included), status updated. Playtest watch points in TODO: scout-vs-drill line match (the promise must be softened if the game breaks it), {newline} rendering, spawn-on-line placement. (2026.07.23 13.08.20)
- [x] TWO BUGS DOWN + THE WHOLE "NEXT UPDATE" BATCH (hero health, the scout leave bar, the 50/50 deal, the honest cooldown clock, the orange-cross opponents, the drill pay-chest). ConfigVersion â†’ 4; all six knobs in config.json AND MCM. (1) HERO HEALTH (Anton: player and companions limped out of drills with real HP loss): new HeroHealthRestorePercent (default 90) â€” LaunchTrainingCore snapshots every hero's HitPoints (both halves) into a transient dict, and the aftermath heals each hero UP to min(pre-drill HP, 90% of max): a bruise may sting, but training never benches anyone â€” and never heals above what they walked in with, so a drill can't be a free hospital (Hero.HitPoints setter is public, verified). (2) THE SCOUT LEAVE BAR (Anton: holding Tab left the mission but showed no "leaving" bar): the bar is MissionLeaveView (Gauntlet override MissionGauntletLeaveView) polling Mission.GetMissionEndTimerValue â€” Mission ITSELF owns the 0.6s hold-to-leave timer for any IsFriendlyMission (default true), the view only draws it, and the borrowed "Camp" view set simply doesn't carry that view; worse, our old ScoutMissionLogic.MissionEnded ended the mission on the key PRESS, before any bar could show. Fix: the scout now opens under its own mission name "TrainingBattlesScout" with our own view set â€” NEW ScoutMissionViews.cs, a [ViewCreatorModule] class whose [ViewMethod] returns vanilla's exact Camp list + ViewCreator.CreateMissionLeaveView(); ViewCreatorManager auto-discovers it (scans every active module assembly referencing TaleWorlds.MountAndBlade.View). FOOTGUN ENGRAVED IN THE CLASS DOC: the scanner does GetCustomAttributesSafe(method)[0] on EVERY static method of a ViewCreatorModule class with no length check â€” one attribute-less static member (even a property getter) crashes the scan; keep that class exactly one method. csproj now references TaleWorlds.MountAndBlade.View (Modules\Native\bin) and SandBox.View (Modules\SandBox\bin), Private=false. The instant-end MissionEnded override is gone â€” vanilla's hold-to-leave owns the exit, matching real battles. (3) THE 50/50 DEAL (new AutoSplitInHalf, default on): opening the divide screen with no pick pre-deals the company â€” every companion and every man flips a fair coin for a side (player never crosses; wounded flip too, tracked separately); guards walk one healthy man across if every coin fell one way, so the Done condition can always be met; still just a PRE-DEAL of the picker's dummy rosters â€” fully editable, Cancel still discards. FOOTGUN: TroopRoster.GetTroopRoster() returns the LIVE internal MBList, not a copy â€” copy it before mutating the same roster in a loop. (4) THE HONEST CLOCK (Anton: show how long until the next drill unlocks): FormatRemaining renders "19 hours and 40 minutes" / "45 minutes" instead of "about N hours", in the muster wait-text and the divide-option tooltip (pressing G during cooldown opens the muster menu, whose first line is exactly this clock). (5) THE ORANGE-CROSS OPPONENTS (Anton: random bandit colors â†’ a chosen banner, orange with a white cross, configurable): new UseOpponentBanner (on) + OpponentBannerCode (any Ctrl+C banner-editor code). Banner-code format decoded from TaleWorlds.Core Banner.TryGetBannerDataFromCode: 10 dot-separated ints per layer â€” mesh.color.color2.sizeX.sizeY.posX.posY.stroke.mirror.rotationDeg on a 1528Ã—1528 canvas centered 764,764; palette ids from Native banner_icons.xml (223 = vivid orange 0xF1580D, 172 = white); no single cross icon confirmable from data, so the default composes one the community way: solid background 11 + two stretched bars of shape 510 (vertical 420Ã—1700 + horizontal 1700Ã—420) â€” WATCH POINT: 510-is-a-bar comes from a community Nordic-flag code, verify the first drill actually shows a cross. Where the colors REALLY come from (verified in Mission.SpawnTroop + MissionCombatantsLogic + PartyGroupAgentOrigin): team flag = combatant.Banner â†’ MobileParty.Banner â†’ PartyBase.CustomBanner (SetCustomBanner âœ“), but uniform tint = Team.Color = MapFaction color pair, and leaderless troops' shield heraldry = MapFaction.Banner â€” so ApplyOpponentLook also dresses the LENDER BANDIT CLAN itself (Clan.Color/Color2/Banner all publicly settable) for the drill's duration, and RestoreOpponentClanLook dresses it back in aftermath AND abort. Crash-safe: the restore data ("clanId|color|color2|bannerCode") rides the SAVE via SyncData, and OnSessionLaunched restores from it â€” clan colors persist in save files, and a crash mid-drill must not leave Calradia's looters flying an orange cross forever. (6) THE PAY-CHEST (Anton: drills should cost X daily salaries, configurable): new TrainingCostWages (default 1, 0 = free) â€” cost = Î£ PartyWageModel.GetCharacterWage(char) Ã— men over the WHOLE company (both halves drill; player excluded) Ã— days; shown in the muster text (idle and armed) and charged at launch via GiveGoldAction.ApplyBetweenCharacters(MainHero, null, cost) with vanilla's floating -gold notification; ReadyToBegin (the new shared gate of Begin-attack/Begin-defend/send-troops) disables with an honest tooltip when the purse is short; an aborted launch REFUNDS the chest in full; a finished drill spends it. Anton's perk-comparison question answered from DefaultItemDiscardModel: with Steward "Paid in Promise" (T4, discarded ARMOR â†’ troop XP) / "Giving Hands" (T5, WEAPONS), donated gear pays 75/150/250/300 XP by item tier â€” cheap T1/T2 gear works out to roughly 1 denar â‰ˆ 1 XP, so a drill's multi-thousand-XP haul priced as donations would cost thousands of denars, ~10Ã— the default one-day-wages fee (a 100-man mid-game party â‰ˆ 600-800/day) â€” training stays the bargain it should be, and the config comment records the math. MCM: 5 new settings including the mod's first SettingPropertyText (verified present in MCMv5). Build clean, 25 tests green, deployed. NOT YET PLAYTESTED â€” watch points: the cross actually looking like a cross, looter colors restored after drill/abort, the leave bar appearing, auto-split feel, cost numbers sane. (2026.07.23 13.54.33)
- [x] BATCH-3 BUGFIXES from Anton's scouting-works-super playtest â€” loot leak, "Regrouping" companions, the battlefield line, cost on the buttons. (1) THE LOOT LEAK (Anton won a drill and KEPT the loot, BannerLoot installed; early builds leaked nothing): diagnosis â€” a regression of the round-8 XP fix. Since then the finalize deliberately drives PlayerEncounter.Update() so vanilla's guarded once-only commit (CalculateAndCommitMapEventResults) settles the XP books â€” but that same commit is the LOOT pipeline, and while OUR reward model zeroes vanilla's loot rolls, Harmony loot mods (BannerLoot) hook the very commit we now let run. Fix that closes EVERY channel: the launch snapshots the baggage train (ItemRoster keyed by item AND modifier â€” a "fine" sword owned before is never confused with a looted plain one), and aftermath step 1d removes exactly what the party GAINED over the snapshot ("N looted items returned â€” there are no spoils in sparring"); losses are never refilled, so nothing can be farmed. (2) "REGROUPING" COMPANIONS (two of Anton's companions stuck): decoded from the decompiled ViewModelCollection â€” "Regrouping" is the clan screen's label for Hero.IsFugitive, and vanilla's map-event end makes the losing side's UNCAPTURED heroes fugitive; our own no-capture guard (GetCaptureMemberChancesForWinnerParties â†’ empty) is exactly what routes drill companions there, and MakeHeroFugitiveAction REMOVES them from the roster â€” which is why the merge-home never saw them. Two-part fix: aftermath step 1c (RecoverScatteredHeroes) walks every alive, non-prisoner hero of either snapshot who is no longer in the main party straight back (ChangeState(Active) + AddHeroToPartyAction; PartyBelongedTo is maintained by the roster merge â€” verified PartyBase.OnHeroAdded â†’ MobileParty.OnHeroAdded â€” so no double-add); plus a ONE-SHOT save repair (synced flag TrainingBattles_FugitiveRescueDone) that on first launch of the fixed build returns any fugitive player companions â€” Anton's two stuck ones â€” then never runs again, so companions who scatter in REAL battles keep vanilla's rules. (3) "Survey the ground" â†’ "Select the battlefield", and the muster text now ALWAYS carries a battlefield line â€” "Battlefield: as fate wills â€” the ground you stand on decides." or "Battlefield: <Describe(scene)> (your choice)." â€” which updates immediately on pick because ChooseGround already re-inits the menu. (4) COST ON THE BUTTONS: all three start options now read "â€¦ ({TB_COST_SUFFIX})" â€” a menu-text variable stamped in the shared ReadyToBegin gate ("(N denars)", empty when free), and the muster text explains it in Anton's asked-for flavor: "It will cost N denars (X days' wages a man) to gather the training materials and pay the men for the drill." Menu-option labels DO substitute MBTextManager text variables â€” pattern now proven in this codebase. 25 tests green, deployed. (2026.07.23 14.22.07)
- [x] Batch-3 round 2 (Anton's screenshot playtest: regrouping/battlefield/cost all confirmed good): the "temp" watermark + the loot leak's second life. (1) THE "temp" TEXT on the muster menu is the engine's placeholder for a menu with no background mesh â€” every vanilla menu init calls args.MenuContext.SetBackgroundMeshName(...); ours never did. Now set to "wait_ambush", the same field-camp image the Company of Trouble quest menu uses (fitting: its battle recipe is ours too). (2) THE LOOT LEAK SURVIVED the snapshot-diff because of WHEN loot actually lands: BannerLoot pushes a loot SCREEN, and a loot screen grants its items only when the player CLOSES it â€” our aftermath (and its sweep) had already come and gone by then, snapshot cleared, diff saw nothing. Fix: the snapshot now OUTLIVES the aftermath â€” RemoveDrillLoot no longer clears it (callers own the lifetime); the aftermath runs a first sweep (catches synchronously-granted loot), then the application tick runs the FINAL sweep only after the map has been the ACTIVE state for 30 consecutive ticks (~half a second) â€” any loot/inventory/party screen is its own GameState, so the counter simply doesn't run while one is up, and the sweep fires right after it closes, removing whatever was taken ("N looted items returned â€” there are no spoils in sparring"). Nothing player-legitimate can be bought in that half-second on the open map, and losses are never refilled. Abort path resets snapshot + counter. Player-health question confirmed: Hero.MainHero rides the member roster, so the hero-HP snapshot/restore has always included the player alongside the companions. 25 tests green, deployed. (2026.07.23 15.04.26)
- [x] THE MOCK ENEMY + THE V1 SCOPE CUT (Anton's two must-haves for the first version: a custom-composed enemy to drill against â€” "I need this to be able to do proper tests" â€” and V1 shipping SCOUTING, not the split-army drill). ConfigVersion â†’ 5, two new feature gates in config.json AND MCM (new "Features" group, first in the menu): (1) EnableSplitTraining, DEFAULT FALSE â€” the whole divide-your-army drill (divide option, ground select, Begin/send buttons) hides from the muster menu until toggled on; NOTHING was deleted, the code path is untouched and returns the moment the toggle flips, and the muster text builds its feature paragraphs dynamically so the V1 default menu reads as scout-only (muster line + SCOUT paragraph + cancel). (2) EnableMockEnemyTraining, DEFAULT FALSE (the developer option): "Compose a mock enemy to drill against" â€” step one a MultiSelectionInquiry over every culture that fields a troop tree (main cultures first, bandit cultures labeled "(bandits)"; tree = BFS over UpgradeTargets from BasicTroop/EliteBasicTroop/BanditBandit/BanditRaider/BanditChief/BanditBoss, sorted by tier â€” all verified in the decompiled CultureObject/CharacterObject), step two the SAME dummy-roster party screen the split drill uses, left = the enemy being composed (cap 1000), right = a synthetic pool of 500 of EVERY troop of that tree; the previous composition opens pre-loaded, so running the composer once per culture builds a MIXED force (the menu text then calls the culture "mixed", read straight off the roster); Done with an empty left side dismisses the pick. The phantom rules, enforced end-to-end: the mock team is mutually exclusive with the split pick (each displaces the other), the temp party gets its own StringId prefix training_mock_enemy_* and name "Mock Enemy", its men spawn FRESH into the bandit-component party (no man, no XP leaves the real roster â€” the whole company fights on the player's side), the opponent snapshot is an EMPTY roster so the aftermath's restore/XP/scatter arithmetic sees only OUR side, the aftermath DESTROYS the party without merging (merging would MINT free troops â€” the exact reason RecoverStaleOpponentParties got a second, no-merge path for the mock prefix; only the mock party's prisoner wagons walk home, since anyone in them could only be ours), and a new 1b-mock sweep removes phantoms WE "captured" from the prison roster by delta-vs-snapshot (a real pre-drill prisoner of the same troop type stays). Everything else rides the proven drill pipeline unchanged: same cost (the whole company drills â€” set TrainingCostWages 0 in MCM for free testing), same cooldown, same TrainingActive guards, banner, chosen ground, hero-HP restore, loot sweeps, drill report. Begin buttons got side-aware labels via text variables ("you attack the mock enemy" / "your half attacks"). BONUS FIX spotted in passing: the aftermath's 1b prisoner walk-back iterated main.PrisonRoster.GetTroopRoster() â€” the LIVE list â€” while AddToCounts mutated it mid-loop (the engraved footgun; it worked only because the path rarely fires); both 1b loops now iterate copies. Build clean, 25 tests green, deployed. NOT YET PLAYTESTED â€” watch points: the composer's pool screen behaving (dummy rosters, but the 500-per-troop supply is a new shape for it), phantoms truly leaving nothing behind (roster, prisoners, loot), and the V1 scout-only menu reading right. (2026.07.23 15.45.57)
- [x] THE SCOPE REVERSAL, SAME DAY (Anton's playtest: mock battle worked â€” loot and phantom troops disappeared as expected â€” and the toggles behaved as designed; but seeing the scout-only default menu settled the real question): the split-army drill IS the core of the mod â€” it's not cheating, and a player who only wants to scout simply doesn't drill, so hiding the core behind a default-off toggle was the wrong shape. ConfigVersion â†’ 6: EnableSplitTraining default flipped to TRUE and its MCM toggle REMOVED (the "Features" group keeps only the mock-enemy developer toggle); the config.json key survives as a hand-edit escape hatch only. Migration flips any v5 config's false back to true unconditionally â€” v5's default-off lived a few hours, so a false there is the stale default, not a hand pick (same reasoning precedent as the Tâ†’G hotkey and 100â†’75 XP migrations). No behavior-code change â€” the gates simply read the new truth. Build clean, 25 tests green, deployed. Remaining playtest debt on the mock drill (in TODO): deliberate defeat, mixed cultures, a drill-report skim. (2026.07.23 16.12.29)
- [x] THE REAL-BATTLE GROUND TOOLS (Anton's must-have, the Luda save: "when I defend I am able to pick where I stand... and here we can be precise, because the facing directions are also locked"): SURVEY + SCOUT on vanilla's "encounter" menu for real field battles, defender AND attacker, each behind its own toggle (ChooseGroundWhenDefending / new ChooseGroundWhenAttacking, both default ON per Anton; ConfigVersion 6->7, new key = safe default, no migration; MCM "Battlefield" group renumbered, hints rewritten). DefendGroundBehavior.cs RENAMED RealBattleGroundBehavior.cs (save-safe: the behavior carries no SyncData payload). SCOPE DECISION, explicitly superseding the 2026.07.23 "real battles stay strict-patch-only" call: real-battle survey now offers the SAME wider same-terrain pool as the training muster (WiderPoolAt, true local ground marked "this ground" and listed first, "As fate wills" kept) â€” the one-scene-per-patch DATA TRUTH meant strict-only was an option that NEVER appeared (candidates<2 always), which is exactly why Anton saw nothing in the Luda encounter; a pick you can never make is not a fairness rule, it is dead code. THE PRECISION FIND (Anton's instinct verified in MenuHelper.EncounterAttackConsequence line ~358): vanilla feeds the mission PatchEncounterDir = (AttackerSide.LeaderParty.Position - DefenderSide.LeaderParty.Position).Normalized() computed at "Attack!" time â€” and in an encounter both parties stand FROZEN, so reading the same formula at scout time gives the IDENTICAL direction, and with map-patch data BattleSpawnPathSelector is deterministic: the scouted lines, ends and facings ARE the coming battle's, no caveat needed (the training scout keeps its assumed-direction honesty text; the real-encounter scout says "their approach is known, these are the true lines"). Plumbing: CreatePatchAwareRecord grew a direction overload; ScoutMission.OpenForRealEncounter(sceneId, playerSide, direction) passes the player's TRUE side so ScoutMissionLogic stands you on YOUR line (attacker scouts see the attacker line â€” the muster scout keeps previewing the defender's); RealEncounterDirection falls back to the assumed Vec2(0,1) if a leader party is missing. Scout option hidden for naval encounters (no lone ride on open water) and while TrainingActive; survey pick still rides the one-shot TrainingBattlesSceneModel.PendingSceneId, cleared on ANY player map-event end. THE STRUCTURAL BET, flagged as playtest point #1: the scout opens a mission UNDER the live encounter (MissionState.OpenNew while PlayerEncounter is active) â€” the same mission-under-a-menu shape as vanilla's pre-battle conversation (the very Luda "surrender?" chat that started this), campaign time frozen, encounter menu re-activates on return; ours is the first non-conversation tenant of that shape, so the Luda-save test list in TODO leads with leave-scout-and-return-intact. README Pillar 2 rewritten around the real-battle tools; CLAUDE.md scope + layout updated. Build clean, 25 tests green. NOT YET PLAYTESTED â€” the Luda save is the test bench. (2026.07.23 16.51.50)
- [x] ANTON'S 2026.07.24 BATCH â€” four asks plus two mid-session additions, all shipped in one pass: the loot screens skipped, the pay described, the muster text shortened, the battle HOUR pinnable everywhere, the mock composer opened to the whole world, and the tools put in one order on every door. (1) NO LOOT SCREENS AFTER A DRILL (Anton's Workshop-comment premonition: players see the post-battle loot list before the "returned" message and cry dupe-bug): vanilla's item loot is now killed AT THE SOURCE while training â€” TrainingBattleRewardModel overrides GetLootItemChancesForWinnerParties + GetLootCasualtyChances to empty lists (verified in MapEvent.LootDefeatedPartyItems / LootDefeatedPartyCasualties: an empty winner-chance list skips the entire distribution loop, so nothing ever enters PlayerEncounter.RosterToReceiveLootItems and DoLootInventory â€” the post-battle loot screen â€” opens NOTHING; same for the casualty-gear path, which our zeroed GetLootedItemFromTroop had already half-closed). The mission scoreboard was checked too: its only loot ribbon is a contribution-% line needing >1 party per side â€” never true in a drill. Belt-and-braces: FinishTrainingBattle clears RosterToReceiveLootItems/Members/Prisoners between its bounded PlayerEncounter.Update passes, so even third-party residue can't open vanilla's screens; Harmony loot mods that push their OWN screens remain covered by the existing snapshot sweep. (2) THE PAY LINE (Anton's wording, one line): "Drill pay: N denars (X days' wages a man) â€” for equipment (javelins, arrows, upkeep after the battle) and rewards to keep the good fighters motivated", in the muster text and the MCM hint; and the muster front-door text tightened to two-three short sentences per feature paragraph (it had grown too tall). (3) CHOOSE THE TIME OF DAY â€” EVERYWHERE (first asked for drills, then mid-session widened to EVERY battle type; an admitted immersion-breaker: night battles are unwatchable on many screens, streamers suffer): ONE config key BattleTimeOfDay (-1 = campaign clock, default; ConfigVersion â†’ 8, hand-edited hours snap to nearest supported) drives everything. Mechanism verified in the decompiled corpus: vanilla CUSTOM BATTLE handles its five hour choices (6/12/15/18/22) by swapping the whole interpolated campaign atmosphere for a named TOD_* preset + season (BannerlordMissions.CreateAtmosphereInfoForMission â€” the hourâ†’asset table is copied verbatim into our shared AtmospherePresets). Drills and the muster scout get it via CreatePatchAwareRecord's new timeOfDayOverride; REAL battles (field, siege assault, sea) get it via a NEW MapWeatherModel DECORATOR (TrainingBattlesMapWeatherModel, the same BaseModel-chain pattern as the SceneModel gate): every battle mission's record is filled through MapWeatherModel.GetAtmosphereModel (MenuHelper/SandBoxMissions), so the decorator overrides exactly that call â€” only while a player map event is live (no override leaks into town walks or arena) and never while TrainingActive (the drill's own record already carries it). The real-encounter scout inherits the pinned hour through the same gate automatically, so the preview lighting always matches the coming battle. Weather, wind and snow always fall through untouched. Editable from THREE doors writing the one key: a "Choose the time of day" option on the muster menu, the same option on the encounter menu, and an MCM dropdown ("Time of day for battles"; McmBridge maps by index convention, and menu picks push back into the MCM dropdown so the two editors never disagree). (4) MOCK COMPOSER: ALL TROOPS IN ONE SCREEN (Anton: no culture door first): the composer now opens the party screen directly over a pool holding EVERY culture's troop tree at once â€” main cultures first (alphabetical), bandit cultures after, each tree in tier order, deduped ("The world's muster rolls", 500 per troop as before); mixing cultures is now just picking, so the run-it-twice flow and its inquiry are gone. His search-box + tier-filter wish is parked in TODO â€” that needs a custom Gauntlet layer over the vanilla party screen; first check whether the screen's own sort bar suffices. (5) OPTION ORDER (Anton, mid-session): tools before drills, same order on every door â€” muster: Scout, Select the battlefield, Choose the time of day, Divide the men, Compose a mock enemy, the Begin/send trio, Cancel; encounter menu: the same Scout â†’ Select â†’ Choose-the-hour, at indices 4/5/6 before "Attack!". The muster's battlefield line now also carries the pinned hour ("Battles fought at noon (12:00) (your choice)."). Build clean, 25 tests green. NOT YET PLAYTESTED â€” the four watch points are in TODO (no-loot-screen, pinned hour across battle types incl. siege/sea, composer usability, text reading right). (2026.07.24 08.24.36)
- [x] PLAYTEST VERDICT ON THE 2026.07.24 BATCH + THE HOUR MOVES TO THE TOP + README REVISIT. Anton's same-day playtest: "all battles train and mock work super â€” there is no loot, even though I'm using the BannerLoot mod" (a better result than promised: emptying the two loot-chance lists starves vanilla's distribution BEFORE the commit hands anything to hook, so BannerLoot's screen never even fired â€” the belt-and-braces sweep stays anyway), and a NORMAL real battle still loots and behaves fully vanilla â€” the Training gate holds in both directions. One refinement from the playtest: "Choose the time of day" is the thing he sets FIRST each time, so it moved to the TOP of the tool block on BOTH doors â€” muster order now hour â†’ scout â†’ select-battlefield â†’ divide â†’ mock â†’ begin/send â†’ cancel; encounter menu the same at indices 4/5/6 before "Attack!". README revisited to match the shipped reality: the no-spoils guarantee (no loot/prisoner screens after a drill), the drill-pay description in Anton's wording, the mock-enemy composer (all cultures in one picker), "Choose the time of day" as the first tool of Pillar 2 (every battle type, admitted immersion trade), the full current knob list with MCM-as-soft-dependency note, and an honest Status section (playtested: drill loop, mock drill, no-loot-even-with-loot-mods, time-of-day, real-battle ground tools; next: composer search box, siege-menu hour option, companions on scout, garrison, naval). Still open in TODO: pinned hour confirmed in play on siege/naval, composer sort-bar sufficiency. Build clean, 25 tests green, deployed, committed. (2026.07.24 09.10.33)
- [x] THE MUSTER TEXT GOES COMPACT (Anton: "too long" â€” he sketched the exact shape he wanted: a title plus three brief lines, real numbers, no empty big words). BuildMenuText rewritten from feature paragraphs to a fixed four-line stat block: header ("Battlefield setup and Training Battles", or the trimmed team-ready line when a pick/mock force is armed) + "Battlefield: â€¦" + "Time: â€¦" + "Drill: â€¦" (the drill line only when a drill mode is enabled). The finds that made the lines honest: (1) the DEFAULT battlefield is now shown BY NAME, not "as fate wills" â€” TrainingGroundPool's localCount tells us when exactly one scene claims the patch (the usual case per the one-scene-per-patch data truth), so the line reads "Battlefield: Plain, open ground (battle_terrain_a) (default â€” this ground)"; only a true multi-candidate patch says "random on this kind of ground". (2) The SURGEON got a name: MobileParty.EffectiveSurgeon (verified in the decompiled MobileParty.cs:836) + GetSkillValue(DefaultSkills.Medicine) renders "10% of the fallen wake wounded, cut further by Surgeon Aeron (Medicine 80)" â€” the player finally sees WHO softens the roll and how good they are; falls back to "the surgeon's Medicine" if the lookup throws. The drill line ("Training battle: â€¦") carries the whole contract in one breath, each piece only when it applies: wounded% + surgeon (Anton's follow-up call: NO "nobody dies" safety note â€” let players take the drill seriously even though nobody will die) + XP kept% + cost ("N denars (1 day's wages a man) for training equipment and troop rewards") + "Party becomes disorganized." + the cooldown ("Next drill in 5 hours and 12 minutes." while resting, else "One drill per 24 hours."). CASUALTIES of the rewrite, deliberate: the DRILL/MOCK/SCOUT explainer paragraphs (the options' tooltips already carry the long form), the separate cooldown-blocked text (the wait now lives inside the drill line; the options stay disabled with their honest tooltips), and DescribeChosenGround (split into BattlefieldLine + TimeOfDayLine â€” the hour reads "Time: noon (12:00) (your pick)." or "Time: the campaign clock (default)."). Build clean, tests untouched (Core unchanged). (2026.07.24 09.32.00)
- [x] PUBLISHED TO THE STEAM WORKSHOP â€” item 3770681619, created Private for Anton's pre-flight look. The whole ImmersiveAI release rig ported (tools/: package.ps1, WorkshopCreate.xml, WorkshopUpdate.xml, WORKSHOP-UPLOAD.md): package.ps1 rebuilds dist\TrainingBattles from scratch under the REAL module identity (Id TrainingBattles â€” deploy.ps1's .Dev rename never ships) with exactly three DLLs (TrainingBattles, TrainingBattles.Core, Newtonsoft.Json; no Harmony, no GUI folder in this mod) + a versioned zip named from the manifest. SubModule.xml bumped v0.1.0 â†’ v1.0.0 for the first public release. Upload path is Bannerlord's own TaleWorlds.MountAndBlade.SteamWorkshop.exe riding the logged-in Steam client â€” game version tag v1.4.7 confirmed against the game's own bin\Win64_Shipping_Client\Version.xml (not assumed from ImmersiveAI's files). All the 2026.07.13-decompiled uploader quirks honored and re-confirmed live: <Tasks> as the file's first node, "Item created. Item ID is 3770681619" + "Uploading done!" as the success signal, the harmless press-any-key crash at exit (exit code 82 â€” ignored by design). Thumbnail Screenshots\preview_thumbnail.jpg (250KB, under Steam's 1MB preview cap) uploaded as the item image; ItemDescription is a working one-paragraph pitch (both pillars + mock enemy + MCM note) â€” the full BBCode description is Anton's to polish on the item page, where Private â†’ Public also flips (Owner Controls; WorkshopUpdate.xml deliberately carries no description/visibility so page edits are never stomped). WorkshopUpdate.xml pre-filled with the item id and the v1.4.7 tag; .gitignore already covered dist/ and the uploader droppings. Release loop from now on: package.ps1 â†’ edit ChangeNotes â†’ run the exe against WorkshopUpdate.xml â€” three commands, documented in tools/WORKSHOP-UPLOAD.md. Post-publish sanity: subscribe, enable the plain "Training Battles" (never together with the .Dev copy), smoke-test the exact build subscribers get. (2026.07.24 12.44.45)
- [x] ANTON'S TWO PLAYTEST BUGS OF 2026.07.24 EVENING â€” the "noon at midnight" mystery and the demoted officers. (1) TIME OF DAY "NOT SYNCED" (Anton: sea-raider battle at night, picker says "noon â€” current", battle opens in full sun): NOT a sync bug â€” the live config.json holds BattleTimeOfDay: 12, so the sky did exactly what the standing pin asked; the default was -1 from the key's first commit and nothing in code writes 12 unasked (picker writes only a click, MCM pull maps indexâ†’hour correctly, migration only snaps non-(-1) values). The REAL defect was the label: "â€” current" on the pinned option reads as "the CURRENT TIME is noon" when it means "your current SETTING", and at midnight that mislabel looks like a broken clock. Fix in ShowTimeOfDayPicker: the marker is now "â€” your pick", the campaign-clock entry shows the honest live clock ("Campaign clock (now 21:00)", via CampaignTime.Now.GetHourOfDay), and the dialog says outright the pick is a STANDING setting holding for every later battle until changed; the encounter-menu tooltip now says "Your standing pick: â€¦" instead of "Currently: â€¦" (the muster line already said "(your pick)"). Anton's config still pins noon â€” pick "Campaign clock" once to return to the true night. (2) OFFICERS UNASSIGNED AFTER A DRILL (Anton 90% sure the opposing half's scout lost the post): CONFIRMED in the decompiled corpus â€” Hero.SetPartyBelongedTo calls RemoveAllPartyRolesOfHero on the OLD party the instant a hero changes party, so every companion moved into the temp "Training Opponents" party is silently stripped of scout/engineer/quartermaster/surgeon (and War Sails' first mate/navigator); the losing half's fugitive scatter wipes them a second way (PartyRolesCampaignBehavior). Nothing ever restored them. Fix: SnapshotPartyRoles at launch (before any man crosses; MobileParty.GetHeroPartyRoles per hero â€” both public) into a transient dict, and RestorePartyRoles (MobileParty.SetHeroPartyRole, also public) AFTER everyone is home â€” in the aftermath as step 1c-bis right after the scattered-hero walk-back, in AbortLiveBattle, and in the halves-could-not-be-formed inline unwind (its merge-back had already demoted the crossers). Re-setting a role a hero never lost is a harmless same-name overwrite, and a hero's own old role set can never trip CanAssignMoreRoleToHero's cap (it counts what they already held). Known accepted gap: a crash mid-drill loses the transient snapshot â€” stale recovery brings the MEN home, the posts are three clicks to reassign; not worth a SyncData string for V1. Build clean, 25 tests green. (2026.07.24 21.20.46)
- [x] THE "TRACKING" POPUP GHOSTS LAID TO REST + THE NOON PIN RESET (Anton's live-fire follow-up, same evening: roles fix CONFIRMED WORKING in play; but entering a village popped vanilla's "Tracking: your companion X, who was separated from you after a battle, is currently in this settlement" once per companion). Mechanism decoded from the decompiled corpus: PlayerTrackCompanionBehavior listens to CharacterBecameFugitiveEvent and files every fugitive player companion into a SAVE-PERSISTED private dictionary ("ScatteredCompanions" SyncData); pre-fix drills routed losing-half companions through exactly that fugitive moment, and walking them home via AddHeroToPartyAction is NOT one of vanilla's removal paths (only hire/fire, delayed-teleport and party-creation are) â€” so the stale entries sat in the save firing the popup at every settlement whose walls the "scattered" companion entered WITH the player (hero.CurrentSettlement == settlement is true for the player's own passengers â€” vanilla never imagined a tracked hero riding along). Fix: SweepCompanionSeparationTracker â€” reflection into the behavior's private dict (GetCampaignBehavior<PlayerTrackCompanionBehavior>, no Harmony, silently no-ops if TaleWorlds renames the field), dropping every tracked hero whose PartyBelongedTo == MainParty (a tracked hero RIDING WITH the player is stale by definition â€” this also heals vanilla's own staleness from any rejoin path). Called from three places: OnSessionLaunched (cleans Anton's polluted save on next load), end of RecoverScatteredHeroes (future drills never leave entries), end of RescueStuckFugitiveCompanions. ALSO: BattleTimeOfDay reset 12 â†’ -1 in Anton's live config.json â€” he never knowingly pinned noon (best guess: a leftover from the morning's hour-feature playtest); if the reset happens while a session is running, the in-game picker's "Campaign clock" click is the authoritative route since the game holds its own copy in memory and any MCM save would write the old pin back. Build clean, 25 tests green, deployed. (2026.07.24 21.36.07)
- [x] THE SEA DRILL BUILT (branch `naval-training` â€” Anton: "step by step, the easy solution", ship-divide GUI explicitly deferred to next release as a MUST) + A LIVE REWARD-MODEL BUG FOUND AND FIXED IN PASSING. THE RESEARCH FIND that unlocked everything: War Sails ships as its own module `NavalDLC` â€” now decompiled into `..\reference\game-decompiled\NavalDLC\` (377 files) â€” and naval-ness is nearly free: `MapEvent.IsNavalMapEvent => !Position.IsOnLand`, so our existing Company-of-Trouble encounter recipe BECOMES naval by being started at sea; the only hard fork is `CampaignMission.OpenNavalBattleMission(rec)` instead of `OpenBattleMission` on the same MissionInitializerRecord (verified against vanilla's own MenuHelper.EncounterAttackConsequence naval branch, whose record recipe our CreatePatchAwareRecord already mirrored field-for-field). "Sinking" is `DestroyShipAction` = `ship.Owner = null` â€” the Ship OBJECT survives with hull/upgrades/figurehead intact, so resurrection is re-own + re-heal, the same shape as our fallen-men restore. THE PASSING BUG (real, live in v1.0.1 for War Sails players): TrainingBattleRewardModel extended DefaultBattleRewardModel, and AddModel-by-base-type means it REPLACED NavalDLC's own `NavalDLCBattleRewardModel` in every REAL battle â€” no figurehead drops, no ship distribution among winners, no naval capture-chance perks, silently. Rewritten as a true DECORATOR (extends abstract BattleRewardModel, delegates all 26 members to BaseModel â€” the same pattern the SceneModel/MapWeatherModel gates already used; the reward model simply predated the lesson), with the training guards kept and FIVE new naval guards while TrainingActive: GetFigureheadLoot -> null, DistributeDefeatedPartyShipsAmongWinners -> empty (also prevents its figurehead-stripping side effect), CalculateShipDamageAfterDefeat -> 0 (vanilla rolls 20-50% hull damage onto the defeated side's survivors), GetSunkenShipMoraleEffect -> 0, GetWinnerPartiesThatCanPlunderGoldFromShips -> empty. THE SPLIT: new Core `FleetSplitMath.OpponentShips` (pure, 9 new tests, 34 green) â€” deterministic greedy fill proportional to each side's healthy crew, largest hulls dealt first to the side furthest under its target share, ties to the player, the flagship (highest `Ship.FlagshipScore` â€” there is no hard flagship property in this game version) NEVER crossing, both sides guaranteed a hull. The muster text PROMISES the exact split before Begin ("The fleet divides with the men: 2 hulls opposite, 3 with you (the flagship yours).") by running the same math on the same inputs. THE BEHAVIOR: `MainPartyAtSea()` (= MobileParty.IsCurrentlyAtSea; at muster time PlayerEncounter.IsNavalEncounter() is always false since CanMusterNow forbids an encounter â€” that call in TrainingGroundPool was quietly useless) now drives the scene pool (BattleSceneCatalog already took isNaval â€” past-us was kind); SplitFleet snapshots every hull's (Ship, HitPoints, SailHitPoints) then transfers via the public Ship.Owner setter (FOOTGUN: the setter edits the party's LIVE ship list â€” copy before looping, the GetTroopRoster lesson again) and sets opponent.IsCurrentlyAtSea; the opener forks on the LIVE map event's verdict (navalEvent) so scene and opener can never disagree; RestoreFleet (aftermath AND abort, always BEFORE DestroyOpponentParty) re-owns every snapshot hull to the main party â€” crossed, "captured" (PlayerEncounter.CapturedShipsInEncounter) or sunk alike â€” and heals hull+sail to the walked-in values, never above; ReclaimShips in the stale-party recovery walks save-persisted hulls home after a crash (ownership rides the save; hurt hulls stay hurt â€” no persisted snapshot to heal from, accepted V1 gap, same reasoning as the roles snapshot). GATES, all with honest tooltips: split drill at sea needs >=2 hulls; mock enemy at sea blocked ("Phantoms do not sail yet") â€” phantom FLEETS are a TODO of their own; muster scout hidden at sea (same "no lone ride on open water" rule as the real-encounter door â€” a sea scout needs a ship-walk mission, TODO); ReadyToBegin re-checks both for picks carried between shore and sea, and LaunchTrainingCore hard-guards them. KNOWN TOP PLAYTEST RISK: naval battles are MULTI-ROUND with a disengage state (IsNavalEncounterFinishedWithDisengage, own "naval_encounter_disengaged" menu, and an instant-loss rule for any side holding zero ships) â€” our positive-kill finalize should short-circuit all of it, but this is exactly the class of vanilla state machine that has bitten before. Build clean, 34 tests green, NOT deployed (Anton was likely in-game) and NOT YET PLAYTESTED. (2026.07.25 09.50.57)
- [x] THE SIDE CHOICE FOR EVERY ROAD (Anton, mid-naval-playtest: "when using auto-resolve in the train battles there is no option to attack/defend â€” same for navy"). Confirmed in the code: the side rode ONLY on the two Begin options; "Send the men in" hardcoded playerDefends:false, so every hill-watch quietly made the player the attacker, land and sea alike. Fix in Anton's own sketched shape â€” choose the side once, then pick the road: new muster option "Choose your side â€” you attack / you defend" (one click flips a transient _playerDefendsChoice, menu re-inits so every label tells the new side; resets to attack each session; shown whenever a drill mode is on, so the side can be set before or after dividing), the two Begin options COLLAPSED into one "Begin the battle â€” your half attacks/defends (N denars)" (mock wording kept: "you attack/defend against the mock enemy"), and "Send the men in" now honors the same choice. Menu got one option SHORTER despite the new feature. Both muster heads now read "Set your side and begin â€” or call it off." Old TB_opt_attack4/TB_opt_defend4 localization ids retired; new TB_opt_side/TB_opt_begin/TB_tip_side. Fixes the naval door for free (same menu). Build clean, 34 tests green. (2026.07.25 10.10.56)
- [x] v1.1.0 SHIPPED TO THE WORKSHOP â€” NAVAL TRAINING BATTLES LIVE, same day as built. Anton's playtest verdict on the sea drill: "won, defeat, auto-resolve, for the old bugs - everything is TOP!" â€” the multi-round/disengage risk (the list's #1) did not bite; the positive-kill finalize holds at sea. Release run: naval-training MERGED to main (clean fast-forward, two commits), SubModule.xml v1.0.1 -> v1.1.0, README Pillar 1 got the at-sea bullet + a naval line in Looking-for, package.ps1 -> dist zip v1.1.0, TaleWorlds uploader -> "Uploading done!" on item 3770681619. ONE RELEASE-CRITICAL CATCH made during the version bump: SubModule.xml never declared NavalDLC, so nothing guaranteed the DLC's models register BEFORE ours â€” loaded the other way round, NavalDLCBattleRewardModel would sit ON TOP of our decorator and its non-delegating overrides (figurehead loot, ship distribution, post-defeat hull damage) would fire during drills, quietly bypassing every naval training guard. Fixed with an optional DependedModuleMetadata NavalDLC LoadBeforeThis (same shape as the MCM soft dependency; module id verified against the DLC's own SubModule.xml). Change notes to players name the naval feature, the new side choice, AND the v1.0.x reward-model bug fix with an honest apology (War Sails players silently lost figurehead drops and ship loot in real battles). Ship-divide GUI stays the next release's must; the plain-DLC-sea-battle regression check (figureheads back in REAL fights) is in TODO â€” the playtest covered drills, not a vanilla sea fight. (2026.07.25 10.28.02)
- [x] THE DIM EAGLES â€” the opposing half fought demoralized (Anton's scoreboard screenshots, land AND sea: the attacker side's morale birds grey, the defender's bright, in both a mock drill and a naval split drill). The wire, traced through the decompiled corpus: every agent's INITIAL battle morale carries (party morale - 50) / 2 via CampaignAgentComponent.GetMoraleAddition (the campaign-to-mission morale bridge; SandboxBattleMoraleModel then only adds perks and the disorganized factor), and MobileParty.Morale = 50 base + RecentEventsMorale + leader-skill effects - starvation - overcrowding etc. (DefaultPartyMoraleModel.GetEffectivePartyMorale). Our temp party is a freshly minted BANDIT party: no food -> Party.IsStarving penalty, tiny bandit party-size limit vs a whole half crammed in -> overcrowding penalty, no leader -> no skill bonuses â€” rock-bottom party morale, so the player's OWN MEN fought ~20-30 initial morale worse from the opposite bank and routed early; the player's half meanwhile carried its real earned morale. Fix: MatchOpponentMorale after the men cross (the party-size penalty depends on the roster the model sees) â€” one RecentEventsMorale addition levels opponent.Morale to main.Morale, the same knob vanilla's own events turn, clamped +-100 by its setter (plenty for the bandit penalties). Both halves now walk in at the company's true morale; mock phantoms get it too (equal footing is the test bench's whole point). No restore needed â€” the temp party dies with the drill. Committed to main, NOT yet released to the Workshop (rides with the next update unless Anton wants a v1.1.1 hotfix). Build clean, 34 tests green. (2026.07.25 10.53.47)
- [x] THE CAPTIVE COMMANDER â€” losing a NAVAL drill took the player PRISONER (Anton's screenshots: "Your captors have been dispersed, and you are able to escape" + "Your party is stranded at sea" + the summary reporting "Drill XP kept: 0"). The chain, decoded from PlayerEncounter: a defeated player walks DoApplyMapEventResults -> PlayerTotalDefeat -> DoPlayerDefeat, which activates vanilla's "defeated_and_taken_prisoner" menu -> real captivity in the TEMP PARTY's wagon; our aftermath then destroyed that party (= "captors dispersed"), and for a beat the party sat at sea shipless (= the "stranded" flash). On LAND the defeat wrap needs a menu tick and our trigger (b) preempts it â€” at sea the road is faster and won every race; the "XP kept: 0" is PROBABLY the clamp, not the diversion (correction after a code re-audit, same day: DoApplyMapEventResults COMMITS the books before it picks the defeat state, so the commit likely ran â€” but a total defeat annihilates whole stacks, and PartyBase.OnXpChanged clamps each stack's XP to men Ã— upgrade cost, so the visible drill EARNINGS evaporate before the snapshot diff can see them; stored pre-drill XP is restored absolutely either way. Expect defeat XP to stay LOW for wiped stacks even after the fix â€” survivors' earnings survive, the fallen's don't). THREE-PART FIX: (1) new finalize trigger (c) â€” the moment the drill's battle is OVER (mission closed or hill-watch simulation done) and the encounter still lives, finalize on that first tick, before vanilla's states can walk anywhere; gated by a new _battleRan flag (set only while the mission/simulation truly runs) so it can never fire in the frames before the mission opens; (2) the winner stomp went UNCONDITIONAL â€” FinishTrainingBattle reads the honest result into playerWon first, then SetOverrideWinner(player side) even over a real defeat (the old "never stomp a real result" HasWinner guard protected exactly the road that must close; every reward channel is zeroed while training, so the stomp buys and costs nothing) â€” the Update-drive then settles the XP books through the same victory road the tested wins use, fixing "XP kept: 0" on defeats too; (3) the belt â€” after Finish, if Hero.MainHero.IsPrisoner the captivity ends via EndCaptivityAction.ApplyByReleasedAfterBattle BEFORE the captor party is destroyed: no dispersed-captors theater, no stranded flash. Build clean, 34 tests green, committed to main. RE-TEST list for Anton: lose a sea drill (fought AND hill-watch) â€” expect no captivity, an honest "the other half carried the field" summary, and NONZERO drill XP; lose a land drill once for regression. Workshop: rides with the morale fix (v1.1.1 or the GUI release). (2026.07.25 11.34.30)
- [x] THE WOUNDED KNOB LEARNS THE DIFFERENCE BETWEEN DEAD AND DOWNED (Anton, after the captivity fix: losing a drill left ~20 wounded from a 150-man company with only ~20 scoreboard KIA â€” expected ~2 at the 10% knob; confirmed on LAND too, so not naval). Root: the round-9 semantics fed the wounded filter dead AND battle-wounded â€” right on a WIN (the two together approximate the men who dropped, since vanilla converts most mission KIA to roster-wounded before we run), catastrophic on a LOSS: the entire beaten side comes home roster-wounded (every man was downed or KO'd), so the knob chewed on all 150. NEW SEMANTICS: only the TRULY DEAD (roster-removed, = MapEventParty.DiedInBattle by construction) enter the two-roll filter; every battle-wounded man is HEALED by the round-9 absolute wounded-target (the negative adjust) unless the filter re-wounds him â€” the men a drill may leave wounded are the men the battle would have PERMANENTLY cost, the KO'd were sparring and shrug it off. This also retro-satisfies round 9's original complaint (14 KIA at 10% now yields ~1-2 wounded, not 14, and not the pre-round-9 bypass either). Summary line now speaks both numbers ("8 fell and 130 were battered â€” 2 wake up wounded, the rest shrug it off"); drill report logs healed stacks too. Build clean, 34 tests green. RE-TEST: lose big on land and at sea â€” wounded should now track the SCOREBOARD-KIA magnitude x knob, and the battered majority wake whole. (2026.07.25 11.45.53)
- [x] THE ROSTER HOLE IS NOT A GRAVE — round 2 of the too-many-wounded loss (Anton: still ~140 'fell' and 6 wounded from ~10 real KIA; the previous fix assumed a lost side comes home roster-WOUNDED — it does not). The true mechanism, finally: vanilla hands the DEFEATED side's downed men to the winners as PRISONERS, and our no-capture guard (GetCaptureMemberChancesForWinnerParties -> empty) leaves that distribution WITHOUT A RECEIVER — the men are REMOVED from the roster and received by nobody, so on the roster a KO'd man and a dead man leave the same hole, and the filter counted them all as fallen. The honest source exists in the event itself: MapEventParty.DiedInBattle (public TroopRoster, filled by OnTroopKilled for exactly the men whose survival roll failed). New HarvestBattleDead reads every event party's DiedInBattle into a per-character dict AT MapEventEnded — which fires on every finalize road: trigger (a) naturally, (b)/(c) synchronously inside FinishTrainingBattle's own PlayerEncounter.Finish, always BEFORE the aftermath arithmetic. The knob's input is now min(event's true dead, roster hole), with the plain roster-diff as fallback when the harvest never ran (never worse than before); the vanished would-be-captured are restored HEALTHY and counted as battered alongside the roster-wounded ('10 fell and 129 were battered — 1 wakes up wounded'). Drill report grew a died column and a harvest-source line, so the next wrong number names its own witness. Build clean, 34 tests green, deployed to the game per Anton's ask (deploy is now part of the fix loop — he lost one retest round to a forgotten deploy). RE-TEST: lose big on land AND at sea — wounded should now be knob x true KIA; check last_drill_report.txt says 'harvest event-DiedInBattle'. (2026.07.25 11.55.59)
- [x] v1.1.1 SHIPPED — the day's three drill fixes live, all playtested by Anton across defeats/wins/land/sea ('Wounded are as expected each time'): (1) the opposing half fights at YOUR party's morale, (2) a lost drill can never take the player prisoner or strand the party at sea, (3) wounded follow the event's true death book only (KO'd men shrug it off). SubModule.xml v1.1.0 -> v1.1.1, short change notes per Anton's call ('nobody reads that much today'), package.ps1 -> uploader -> 'Uploading done!'. (2026.07.25 12.02.41)
- [x] THE OFFICER UPDATE - three of Anton's NEXT UPDATE calls in one pass (config v9): the mock enemy graduates, the quartermaster earns the XP, and the scout earns the ground. (1) MOCK ENEMY ON BY DEFAULT - EnableMockEnemyTraining default flipped to true, "(developer)" dropped from the MCM label, and a v9 migration flips any older config carrying false (that was only ever the old default - same call as v6's split-training flip). (2) QUARTERMASTER SCALES THE KEPT XP - the flat XpKeptPercent is replaced by a band: XpKeptMinPercent (def 50) at Steward 0 rising LINEARLY to XpKeptMaxPercent (def 150) at Steward 300, read off MobileParty.EffectiveQuartermaster (vanilla falls back to the leader when none is assigned - verified in the decompiled source). Steward 150 -> 100% kept, per Anton's example; past 100% the drill GRANTS bonus XP (AftermathMath.XpKept's clamp widened to 0..MaxKeepPercent=300, XpToRemove going negative = bonus to grant - the absolute-restore aftermath handles it unchanged, and the OnXpChanged clamp naturally caps runaway pools). Pure math in AftermathMath.XpKeptPercentForSkill (10 new test rows); the percent is computed ONCE per aftermath and stamped into last_drill_report.txt's header with the quartermaster's name, skill and band, the muster line and pick tooltip now read "N% XP kept (Quartermaster X, Steward Y)", and the summary prints "(at N%)". MIGRATION HONESTY: a v8 XpKeptPercent hand-set away from 75 was a deliberate pick, so it migrates as a FLAT band (min=max=the pick) via a nullable leftover property (NullValueHandling.Ignore - never written back); the untouched default takes the new 50..150 design. Set both sliders equal for a flat rate - no extra toggle needed. (3) THE SCOUTING DUEL - on a REAL field battle's encounter menu, choosing the battlefield and pinning a non-daylight hour must now be EARNED: your party's EffectiveScout Scouting vs the enemy side's BEST effective scout (max across its parties - an army is screened by its sharpest outriders), at ScoutingGateDefendPercent (def 75 - you already hold the ground) when defending and ScoutingGateAttackPercent (def 125 - dictating the field takes a real edge) when attacking. Lost duel = the survey option shows LOCKED with the honest numbers ("your scouting is X and theirs is Y - it needs to be at least Z"), per Anton's spec, and the shared time-of-day picker (new optional hourLock param on BattleSceneCatalog.ShowTimeOfDayPicker) locks every hour EXCEPT the campaign clock and FULL DAYLIGHT (noon, ModConfig.FullDaylightHour) - Anton's QoL rule for streamers and dark screens; the muster's picker stays free (no enemy, no duel). The lone scout RIDE stays ungated (it reveals but changes nothing) - flagged as an open question in TASKS_TODO along with the standing-hour wrinkle (a pre-pinned hour still applies to a lost-duel battle; the gate stops changing it from that menu, not the standing key). ScoutingGateEnabled=false restores the free-for-all. Pure ratio math in new Core ScoutingMath (RequiredSkill = ceil of enemy x percent/100 - the bar must truly be reached; 16 test rows; enemy skill 0 always passes). Per Anton's mid-session call, ALL FOUR tuning knobs (QM min/max, defend/attack ratios) are MCM sliders with the design defaults spelled out in the hints, plus the gate toggle. 63 tests green, build clean. NOT YET PLAYTESTED; deploy when the game is closed. (2026.07.25 12.49.15)
- [x] THE OFFICERS UPDATE PROPER - Anton came back from a walk and the same-day patch grew into the design he actually wanted: EVERY number in a drill is now owned by an OFFICER, land and sea, with the naval professions joining the table (config v10). THE MAP (the new Officers.cs, one authority): drill XP = QUARTERMASTER judged by LEADERSHIP on land (Anton's explicit call over Steward, confirmed via question - the player's own Leadership when nobody holds the role) and FIRST MATE judged by War Sails' BOATSWAIN at sea; the ground duel = SCOUT/Scouting on land, NAVIGATOR/SHIPMASTER at sea; casualties = SURGEON/Medicine everywhere. RESEARCH FIND for the corpus: the naval roles live on MobileParty in CampaignSystem itself (EffectiveFirstMate/EffectiveNavigator, leader fallback), but their skills are DLC objects - NavalSkills registers "Mariner"/"Boatswain"/"Shipmaster" by string id and the perk trees prove the role mapping (Boatswain-tree perks carry PartyRole 14 = FirstMate, Shipmaster-tree 15 = Navigator); Officers.cs looks the skills up via MBObjectManager by id so the mod NEVER references NavalDLC.dll - no DLC, no crash, land officer steps in. XP BAND RETUNED: 40% at skill 0 to 100% at 300 (Anton's new defaults), slider cap 200 (he capped it at 200 knowing bonus XP already worked); AftermathMath.MaxKeepPercent 300->200, the const renamed OfficerSkillCap. THE SURGEON'S THREE BANDS replace both the flat WoundedPercent knob AND the old vanilla-survival-chance save (AftermathMath.WoundedAmongFallen deleted): each scales LINEARLY with Medicine 0..300 via the new ChancePercentForSkill (double ends, either direction, fractions welcome) - (1) KIA->KIA "real death": 3% -> 0.1%, THE DRILL'S ONE REAL PERMANENT COST, a deliberate break of the founding "nobody ever dies" pledge (Anton's design; CLAUDE.md hard requirement AMENDED - it is announced in the muster tooltip and the summary shouts "N men TRULY DIED", and both ends at 0 restore the old pledge); (2) KIA->wounded: 30% -> 5%; (3) downed->wounded: 15% -> 1% (the KO'd/battle-wounded pool, previously always healed - now a few keep their bruises); NO wounded->death path, per Anton, and heroes stay exempt. New Core: JudgeFallen (per fallen man: death roll then wound roll, ALWAYS two rolls so the random stream position is outcome-independent - tested) returning a FallenVerdict struct, StayWounded (one roll per downed man). The aftermath restores men-first MINUS the truly dead (comeBack = fallen - died), wounded target = pre + kiaWounded + stayWounded, surgeon credited only for the men he actually saved (casualties - died). COSTS SPLIT BY GROUND: TrainingCostWagesLand (1) / TrainingCostWagesSea (2, Anton: sea drills cost double) - CostDays() picks by MainPartyAtSea, menu lines say "sea rates", and the config comment reserves the castle (~10x)/city/army rates for those future drills. LOGGING (Anton: "make sure we can easily debug"): new TbLog - a rolling training_battles.log beside config.json (timestamped [area] lines: config loads with the full band picture, drill begin with both officers resolved, drill end with every total and the exact chances used, duel verdicts logged ONCE per change not per menu frame, ground/hour/scout picks), ~1 MB self-trim at a line break, best-effort try/catch everywhere, MCM "Debug logging" toggle wired live through the bridge (TbLog.Enabled), default on; last_drill_report.txt stays the per-stack witness and now opens with the officers and their computed chances. MCM REBUILT AS AN OFFICERS TABLE (Anton's mid-walk note: defaults visible, tunable, readable): groups "Features" / "The Quartermaster - drill XP" (2 sliders) / "The Surgeon - the fallen" (6 float sliders - SettingPropertyFloatingInteger, pattern proven in ImmersiveAI - plus hero heal) / "The Scout - the ground" (gate + 2 ratios) / "Pacing & costs" (cooldown, land cost, sea cost, auto-split, disorganized) / "Battlefield" / "Controls" / "Debug"; every hint names the officer, the skill, both ends and the default; the engineer gets a group when castles land. MIGRATIONS (v10, all leftover-key pattern - nullable, NullValueHandling.Ignore, never written back): v9's short-lived 50/150 XP defaults follow to 40/100 (hand picks stay, clamp pulls >200 down), hand-set WoundedPercent becomes a FLAT KIA->wounded band, hand-set TrainingCostWages keeps land and doubles for sea. DECIDED on the walk: the lone scout RIDE stays free forever (ride reveals, ground choice decides); the standing-hour softness stays noted in TASKS_TODO. Duel messages now name the real officer ("your Shipmaster is 40 against their 90..."). 75 tests green (JudgeFallen stream-position test, band direction tests, cap tests), build clean. NOT YET PLAYTESTED; deploy when the game is closed. (2026.07.25 15.36.44)
- [x] v1.2.0 SHIPS - Anton's playtest verdict ("everything else works good, tested!") plus four retunes, then the release loop. (1) THE ONE-BATTLE HOUR: the mystery of Anton's sky stuck at noon solved - through v10 every menu "Choose the time of day" pick WROTE the standing config key, so a one-off pick silently became the default. Now the menus arm TrainingBattlesMapWeatherModel.PendingBattleHour (static, never saved): one battle only, cleared at player-map-event end and session launch, read everywhere through the new EffectiveBattleHour(config) = pending ?? config (drill records, muster scout ride, the real-battle weather gate, every label). Only MCM writes config.BattleTimeOfDay now (the dropdown renamed "standing default"; McmBridge.TryPushBattleTimeOfDay deleted - nothing to mirror anymore); picker texts say "NEXT battle" and the v11 migration resets any stored hour to -1 ONCE (pre-v11 values are likely pollution, post-v11 values are trusted MCM picks). This also RESOLVED the "standing hour beats a lost duel" softness from AI_NOTES - the only standing hour left is the player's deliberate MCM default, ungated by design. (2) Duel ratios retuned: defend 75->50, attack 125->150 (defaults + MCM + v11 migration for values still on the old defaults). (3) Surgeon Medicine-0 ends eased: KIA->wounded 30->20%, downed->wounded 15->10% (same migration pattern; the Medicine-300 ends and the real-death band untouched). (4) RELEASED: SubModule.xml v1.1.1 -> v1.2.0, WorkshopUpdate.xml change notes retold as the officers update, package.ps1 -> dist\TrainingBattles_v1.2.0.zip, uploaded to Workshop item 3770681619 via the official uploader. 75 tests green; deployed to .Dev as well. (2026.07.25 16.41.22)
- [x] THE CUSTOM GUI ERA OPENS — the mod's first Gauntlet windows, and with them the sea drill's ship divide and the mock enemy's phantom fleets, plus the last missing hour-pick doors. NO HARMONY needed (Anton asked whether we must go the ImmersiveAI road: their own chat/letter windows prove custom windows are pure public API — GauntletLayer + LoadMovie over a ViewModel, prefab XML under module\GUI\Prefabs, native brushes/sprites only; their one Harmony patch serves an unrelated map-notice; if we ever must inject into VANILLA screens, that is when Harmony joins, one light patch at a time). The shared frame: `UI\TrainingWindow` — one modal window at a time over ScreenManager.TopScreen (the muster menu lives on the map screen, so the window lays right over it), Escape polled from SubModule's tick and routed to the VM's Cancel, teardown best-effort; deliberately generic so the future siege-equipment picker is just a new VM + prefab. WINDOW 1, "Divide the ships" (`ShipDivideVM` + `ShipDivideRowVM` + TrainingBattlesShipDivide.xml, muster option `training_ships`, afloat + ≥2 hulls + men divided): two columns (with you / opposite), a click sends a hull across, the flagship is pinned and says so, per-side footer weighs berths vs men (warning tint when crowded), "As the men divide" resets to the FleetSplitMath auto-split. Key semantic: confirming the UNTOUCHED default stores NULL — "follow the men", so a later re-division of the company re-divides the fleet too; any hand-moved hull makes the pick explicit (these very hulls). SplitFleet honors the pick via ManualCrossingIndices — validated against the live fleet at Begin (hulls sold/sunk since picking are dropped with a message; a pick gone empty falls back to auto), the muster's fleet-preview line says "at your word" for a manual pick, and the pick clears once the drill launches. WINDOW 2, "Lay down the phantom fleet" (`FleetComposeVM` + `FleetComposeRowVM` + TrainingBattlesFleetCompose.xml, muster option `training_mock_fleet`, afloat + mock enemy composed): every hull class any culture's yards launch (Culture.AvailableShipHulls, main cultures first, deduped) with +/− tallies, hard cap 12 hulls (mission perf), a fleet-wide fittings tier cycler (bare / harbor I / II / III) and the honest berths-vs-phantoms footer; confirm with no hulls clears. MOCK ENEMY NOW SAILS: the at-sea block is gone; Begin at sea instead demands a laid-down fleet (a shipless side loses the naval event INSTANTLY — verified vanilla rule), BuildPhantomFleet conjures `new Ship(hull)` per tally (the recipe vanilla's own naval quests use), marks them IsTradeable=false + IsUsedByQuest, dresses each slot with the best ShipSlot.MatchingPieces piece whose RequiredPortLevel the tier affords (Core `PhantomFleetMath.UpgradePickIndex`, deterministic, 7 new tests — 82 green), re-heals to the raised ceilings, and hands them to the mock party BEFORE StartBattle. The phantoms' hulls are POISON if they leak: every exit road — FinishTrainingBattle, AbortLiveBattle, AND the stale-party recovery — now SinkPhantomFleet (Owner=null, exactly vanilla sinking's leftover shape) instead of reclaiming; the old mock-branch ReclaimShips ("belt and braces — mock drills never lend hulls today") would have MINTED a free fleet the day phantoms sailed, and is gone. The player's own fleet is snapshotted (SnapshotOwnFleet) even in mock sea drills — phantoms hurt real hulls, RestoreFleet heals them — and the aftermath's new SweepForeignHulls dissolves any "captured" phantom vanilla handed over (own-hull set read from the snapshot before RestoreFleet clears it). HOUR DOORS: menu research found naval pre-battle encounters use the plain "encounter" menu (already ours), so the missing doors were `menu_siege_strategies` (added — both siege sides sit there; deliberately NOT duel-gated: when to assault or sally is the commander's own call — Anton may veto) and `naval_encounter_disengaged` (added — multi-round sea fights breathe there; duel-gated like the pre-battle door, "out-sailed" wording). Plumbing: csproj gains the four Gauntlet/ScreenSystem references (Private=false as ever), deploy.ps1 and package.ps1 now ship module\GUI (remove-then-copy against the GUI\GUI nesting trap), SubModule ticks TrainingWindow. Build clean, 82 tests green. NOT YET PLAYTESTED — the muster flow at sea end-to-end and both windows' looks are the first thing to eyeball. (2026.07.25 17.38.57)
- [x] The orange-banner looters unmasked and fixed (Anton's first sea-GUI playtest — everything worked first try, but after a lost sea drill against 250 phantom Huscarls a 25-looter party stood on the coast flying the training banner). The chain: our drill dresses the whole LENDER bandit clan in the training banner (mission uniforms/heraldry read the map faction — the known clan-colors dance), the temp party's destruction drops the lender hideout's bandit count, vanilla's hideout replenisher (BanditSpawnCampaignBehavior, hourly clan tick) answers with a fresh looter party, and map time DOES pass around the drill's edges (the muster wait, the hill-watch simulation, the aftermath ticks) — so the newcomer can be born while the clan still wears orange. The keeper of the disguise: party map VISUALS are built once and never refreshed on a clan-banner change (vanilla never mutates clan banners, so no refresh path exists) — the restore put the clan right but the already-built icon kept the orange until a save/load rebuilt it (PartyBase.OnFinishLoadState → SetVisualAsDirty, which is also why the bug self-healed on reload). Fix, one stroke in RestoreOpponentClanLook: after restoring the clan's colors/banner, walk MobileParty.All and SetVisualAsDirty() every party of that clan — the engine's own on-load heal, run at once, covering land and sea drills alike and the crash-recovery road too (the restore data rides the save). Not player-verifiable-harmful beyond looks — the looter party itself is legitimate vanilla replenishment, only its coat was ours. 82 tests green, deployed. (2026.07.25 17.52)
- [x] THE CASTLE UPDATE — the siege drill lands (Anton: "I got my first castle... lets organize a training, take that castle for a spin"; four design calls made in-session via the decision picker: garrison EMBRACED, engine cost = value × multiplier, engineer skill unlocks engine TIERS, CASTLES first — towns later; renown/influence added mid-session on his follow-up ask). THE RESEARCH FINDS (all in the decompiled corpus, the update's foundation): (1) the siege mission is directly callable — OpenSiegeMissionWithDeployment takes the castle's own scene (LocationComplex "center" at the REAL wall level), the REAL wall-HP array, and the engine lists as plain MissionSiegeWeapon.CreateCampaignWeapon data — no siege camp needed for the mission itself; (2) BUT the mission-end engine writeback (CampaignMissionComponent.OnEndMission → GetLeaderParty(Attacker).SiegeEvent.SetSiegeEngineStates...) NULL-REFS without a campaign SiegeEvent — so the drill creates a REAL one (StartSiegeEvent) whose empty construction lists make the writeback a safe no-op (it walks DeployedSiegeEngines, not the mission list); (3) THE CAPTURE LOCK — MapEvent's private _keepSiegeEvent (vanilla's own "siege continues" switch, public only via FinishBattleAndKeepSiegeEvent which also ends the battle) makes FinalizeEvent skip the whole SiegeCompleted/AfterSiegeCompleted dispatch: set by reflection the moment the event exists, so capture/sack/devastation can never touch the player's own castle on ANY exit road; (4) a defender-side mobile party with CurrentSettlement==null silently FLIPS the event Siege→SiegeOutside (a field battle outside the walls) — defenders must sit INSIDE before joining; (5) PlayerEncounter.StartBattle makes the event a Siege on its own when the defender is a fortification; (6) walls are damaged ONLY by campaign bombardment ticks, never by the mission — snapshot/restore is belt-and-braces; (7) on the ATTACK road the garrison never auto-joins (same faction as the attacker — vanilla's hostility check refuses to defend against their own lord), so SeatFriendliesOnTheWalls sets MapEventSide positively; (8) being inside a settlement IS a PlayerEncounter — the launch stands it down with Finish(false) (keeps the men inside), the aftermath re-opens the castle via a fresh StartSettlementEncounter. THE SHAPE: castle-menu door (owned castles only, per-castle 7-day clock as a SyncData string, honest disabled-tooltips) → the SAME muster menu in siege mode (scout/ground/send-troops hidden — you can walk your own walls; hour pick, divide, mock composer all live) → the ENGINEER'S BENCH (SiegeEquipVM + TrainingBattlesSiegeEquip.xml over the TrainingWindow frame — both sides on one list, tiers by the engineer's Engineering: ram always, ballista+onager @50, tower+fire @100, trebuchet @150, MCM-tunable; caps mirror the mission slots 1 ram/2 towers/4 ranged a side; each engine bills ManDayCost × gold-per-man-day, default 20) → defend road: temp party besieges, you + garrison + militia hold; attack road: LeaveSettlement(you) + EnterSettlement(temp), you storm walls manned by your other half (or phantoms) BESIDE your garrison → aftermath: dismantle the siege (FinalizeSiegeEvent — resets siege state, clears Settlement.SiegeEvent), restore walls, RestoreFriendlyParties walks garrison/militia/guesting lords back IN PLACE by the same surgeon bands and XP-kept rate (the main party's officers — it is their drill), running BEFORE the main pass and CONSUMING its share of the _battleDead harvest so shared troop types are never judged dead twice; Anton's "take the garrison into your party" notification lives in the muster text and the door tooltip. COST: 5× days-of-wages (his call, over the board's old 10× sketch) over EVERYONE on the field — both halves, garrison, militia — plus the engine bill. PRESTIGE (his mid-session ask): a castle drill is a grand public muster — renown + influence per 100 friendly men on the field (defaults 1.0/1.0, MCM 0..10 each), paid ONCE at the aftermath via GainRenownAction/ChangeClanInfluenceAction and NEVER through the battle books (those stay zeroed while training, so kills/loot can never farm it); castle-only for now, extending to field/sea drills flagged as undecided on the board. PLUMBING: ConfigVersion → 12 (8 new keys, all safe defaults), the Engineer's MCM group lands between the Scout and Pacing (the slot the officers update reserved), Officers.EngineerOfficer (EffectiveEngineer/Engineering), Core SiegeDrillMath (TierForSkill, EngineCost, EquipmentBill — overflow-clamped after a red test caught long-overflow) + 9 tests (91 total green), stale recovery grew RecoverStaleDrillSieges (any siege besieged by a training party OR the player besieging their OWN settlement is a drill leftover — finalized before party recovery). PLAYTEST RISKS ranked in AI_NOTES: the attack road's novelties (same-faction walls, guesting lords pushed out by vanilla's inside-parties sweep), the post-battle menu dance, siege defeat roads, phantoms on defender engines, the hour door under siege skies. (2026.07.25 18.56.11)
- [x] THE SEA SCOUT RIDE — the flagship sails free: at sea the muster's scout option no longer hides — it takes the FLAGSHIP out alone onto the chosen naval scene, the player at the helm and as many healthy men as the deck holds rowing along, hold Tab home. ZERO cost, ZERO clock, ZERO XP — Anton's explicit design after the session's research proved vanilla itself teaches nothing here: the ONLY passive sailing XP in War Sails is a campaign-map tick (MobilePartyTrainingBehavior every 4th hour while MOVING at sea → NavalSkillLevellingManager.OnTravelOnWater → the NAVIGATOR alone gains Shipmaster = round(1.4 × party speed), ~40–70 XP/day; the scout explicitly gains nothing at sea, the land "all heroes tick Riding/Athletics" rule doesn't run on water, regular crew gains nothing from sailing EVER), and campaign time freezes inside missions — so the ride is honestly, verifiably free. THE KEY RESEARCH FIND (decompiled NavalDLC.CustomBattle + NavalDLC.View + NavalDLC.GauntletUI fresh into ..\reference\game-decompiled\): War Sails' naval CUSTOM BATTLE proves the whole ship stack runs WITHOUT a MapEvent — NavalMissionState.OpenNew + CustomBattleTroopSupplier + plain MBList<IShipOrigin> (campaign Ship implements IShipOrigin and passes directly); the core ship chain (NavalShipsLogic/NavalAgentsLogic/DefaultNavalMissionLogic/DefaultNavalMissionAgentSpawnLogic) has ZERO MapEvent references, the coupling lives only in the mission-method wrappers. THE BUILD (SeaScoutMission.cs + SeaScoutMissionViews.cs + the ScoutCondition/LaunchScout fork): the custom-battle recipe MINUS every battle part (no battle-end, ship-retreat, collision-outcome, victory, morale, order UI, scoreboard, deployment views) with an EMPTY attacker side — both teams exist because much of the stack indexes them, but nothing spawns opposite; every empty-side road audited in decompile (InitializeShipAssignments early-returns, DeployBattleSide finds no framed plans, Team.ResetTactic self-guards on HasTeamAi) and MissionTeamAITypeEnum.NoTeamAI keeps naval tactics from ever querying the empty sea. The deployment PHASE is skipped — SeaScoutRideLogic hooks DeploymentMissionController.OnAfterSetupTeams and calls FinishDeployment on the spot (Mission.IsDeploymentFinished guards the double call: FinishDeployment removes the controller mid-tick but the pending event invocation still reaches us — read the base class, not guessed). Crew are SYNTHETIC (CustomBattleCombatant, player SetGeneral, roster's healthy men to the DLC's own ShipDeploymentModel.GetMaximumDeployableTroopCountForTeam cap), so mission states never touch the campaign roster. HULL SAFETY verified then belted: MissionShip PROXIES the campaign Ship live (HitPoints => ShipOrigin.HitPoints, damage via ShipOrigin.OnShipDamaged mid-mission), so Open snapshots the flagship's HP/sail-HP and SeaScoutRideLogic heals it home in OnRemoveBehavior (idempotent, every exit road). SOFT DEPENDENCY, THE SECOND McmBRIDGE: NavalDLC.dll + NavalDLC.View.dll referenced at build (NavalDlcBinFolder in Directory.Build.props), never shipped; HARD RULE — naval types ONLY in method bodies, never in a base class/interface/field type/method signature of anything in the module assembly (assembly scans must succeed without the DLC; SeaScoutRideLogic's skeleton is deliberately all-vanilla — DeploymentMissionController and campaign Ship are base-game types); the bodies only run at sea, impossible without War Sails. Views: the land scout's walk-around core + hold-Tab leave bar + the sea's own NavalViewCreator.CreateMissionShipControlView (the helm HUD/input — the whole point) + MissionPreloadView/NavalShipsPreloadView, one method per [ViewCreatorModule] class (the scanner footgun). Build clean, 91 tests green, deployed to .Dev. NOT YET PLAYTESTED — watch-list in AI_NOTES (empty-side boot, helm under our mission name, spawn frames on naval scenes, leave roads, menu return, pinned-hour water). (2026.07.25 21.24.03)
- [x] SEA SCOUT: READMES + THE CRASH HUNT (rounds 1–3, recorder armed for round 4) — while Anton rode, the public pages learned the sea: README + Steam + Nexus bbcode each gained the free-sail line in "Looking for…?", the flagship-scout bullet in Pillar 2 (qualitative only, no numbers — the readme rule), the ship-divide "at your word" line, and the mock-fleet "the phantoms sail" line replacing the stale "coming next" teaser; castle drill deliberately left off the pages until its playtest settles; WorkshopUpdate.xml untouched (written at release). THEN THE MAIDEN VOYAGE CRASHED — three rounds, each dying further in, each diagnosed from the crash folder (ProgramData crashes\<stamp>\rgl_log_*.txt read against a HEALTHY sea-drill log side by side — the diagnosis pattern that worked all night): ROUND 1 died in the loading screen between water_prefabs.xml and the first "Preload physics" line → the view set carried SandBox.View's MissionPreloadView, the CAMPAIGN preloader whose first tick walks MapEvent.PlayerMapEvent.InvolvedParties — and the ride has no map event; fix = MissionCustomBattlePreloadView, vanilla's own preloader for its MapEvent-less naval missions (reads MissionCombatantsLogic.GetAllCombatants — our CustomBattleCombatants exactly); LESSON: for a MapEvent-less mission take the CUSTOM BATTLE variant of any view that has one, the campaign twin may assume the encounter (SandBox.View + TaleWorlds.MountAndBlade.View decompiled into the reference corpus for this). ROUND 2 got teams added, equipment preloaded, THE FLAGSHIP SPAWNED (CreateMissionShip: drakkar_ship_nested) — then died where a healthy drill starts crew work; response = SeaScoutDeploymentController, a subclass of the DLC's deployment controller wearing a FLIGHT RECORDER (TbLog at every phase boundary) plus a self-heal that hand-spawns the player onto the flagship if vanilla's allocation leaves them missing (base SetupTeams derefs Mission.InitialPlayerAgent UNGUARDED — the round-2 suspect); the subclass is the ONE sanctioned break of the NavalDLC bodies-only rule (MCM-settings-class foreign-base precedent, documented on the class and in CLAUDE.md). ROUND 3: the recorder SPOKE — the EMPTY ATTACKER SIDE SETS UP CLEAN (the build's one prototype unknown, retired), the death is INSIDE base OnSetupTeamsOfSide(Defender), downstream of the ship spawn; response (deployed, awaiting round 4) = the recorder now REPLICATES the base's four player-side steps verified against the decompile (DeployBattleSide → AllocateAndDeployInitialTroops → SetupAgentAIStatesForSide → OnSideDeploymentOver; the two internals via reflection, miss falls back to plain base) with a log between each, catches managed exceptions with FULL INNER STACK into training_battles.log, and OnAgentBuild logs every agent by name — if one crewman's build kills, the last line names them. Static audit of the whole spawn chain (AllocateAndDeployInitialTroops force-enables spawning so the ~106-crew batch runs inside step 2; Mission.SpawnTroop, GetAgentTeam, CustomBattleAgentOrigin, NavalTeamAgents.AssignTroops, OnSideDeploymentOver, GetShipAssignment(0, Infantry) — flagship IS formation 0) found no smoking gun — the round-4 log decides. NEXT SESSION STARTS AT: ride once, read the [sea-scout] lines. Strip the OnAgentBuild spam once she sails. (2026.07.25 22.07.28)
- [x] SEA SCOUT ROUND 4: THE RECORDER PAYS FOR ITSELF — one ride, one log read, root cause in hand: step 2/4 (crew spawn), FIRST agent build, `InvalidCastException: CustomBattleCombatant → PartyBase` at `SandboxAgentStatCalculateModel.InitializeMissionEquipment`, full stack straight out of training_battles.log exactly as designed. The disease (verified in the SandBox decompile): the CAMPAIGN's agent-stat model HARD-CASTS every agent's `Origin.BattleCombatant` to `PartyBase` — null flows through, foreign types die — and vanilla never mixes CustomBattleTroopSupplier with a campaign, so nobody had ever stepped on the mine. THE FIX: the ride's own `SeaScoutAgentOrigin` + `SeaScoutTroopSupplier` (pure vanilla interfaces — the soft-dependency rule holds without exception here): BattleCombatant presents the REAL `PartyBase.MainParty` — campaign-native for every model and TRUTHFUL, the crew are the main party's men, so faces (GetPartyMemberFaceSeed), faction colors and perk context all read right — while SetKilled/SetWounded/SetRouted/OnScoreHit are NO-OPS, the free ride's nobody-pays pledge enforced at the origin itself (vanilla's SimpleAgentOrigin was almost the answer, but its SetKilled TRULY kills heroes — corpus note). CustomBattleCombatants stay for TEAM identity and the custom-battle preloader's character walk only; the player is still found by IsPlayerCharacter and helmed via Game.PlayerTroop (campaign sets it to main_hero — Campaign.cs:1318, verified). LESSON, generalizing round 1's: in a campaign, EVERY per-agent game model may assume campaign types — a MapEvent-less campaign mission needs campaign-SHAPED origins, not custom-battle ones. ALSO Anton's naming: the muster scout option is now a text variable — "Scout out the sea with your flagship" afloat, the classic "Ride out and scout a battlefield" ashore (encounter-menu door untouched, it never sails). Built, deployed, awaiting round 5; after she sails, strip the per-crewman build log + the reflection step-replication (keep the phase lines). (2026.07.26 08:45)
- [x] THE SEA SCOUT SAILS — round 5, Anton: "It worked as a charm this time!" Five rounds from maiden crash to open water in under twelve hours: the campaign preloader (round 1), the flight recorder aboard (rounds 2-3), the recorder's full-stack catch (round 4), the campaign-shaped origins (the fix) — and the flagship rides free, crew rowing, zero cost, zero XP, hull healed home. The muster option reads "Scout out the sea with your flagship" afloat. Remaining housekeeping on the TODO: strip the crash-hunt debug spam (per-crewman OnAgentBuild lines + the reflection step-replication in SeaScoutDeploymentController — keep the phase-boundary lines), then the ride joins the normal playtest rotation. (2026.07.26 09:02)
- [x] THE COMPANIONS EARN THEIR KEEP — two polishes from Anton's board (2026.07.26), both riding the officers pattern. (1) THE QUARTERMASTER SPEEDS THE CLOCK: every drill cooldown — the field/sea clock AND each castle's 7-day clock — is now DIVIDED by a factor scaling linearly with the effective Quartermaster's STEWARD (vanilla's own role skill; falls back to the player like every Effective* role): /1 at skill 0 up to a config ceiling at 300 (CooldownDivisorAtSteward300, default 4, clamp 1..10 — set 1 to switch the polish off). Pure math in TrainingCooldown.DivisorForSkill (ceiling below 1 clamps to 1 — the clock never runs SLOWER for having a quartermaster); Officers.CooldownOfficer is the new duty entry (Quartermaster/Steward ashore AND afloat — camp or hold, someone runs the stores); both CooldownReady and CastleCooldownReady divide through EffectiveCooldownHours, read LIVE so promoting a steward mid-wait bites at once; the muster tooltip names the discount ("One drill per 18 hours (Quartermaster Ansif (Steward 75) speeds the camp)") via CooldownNote. (2) THE FIGHTER COMPANIONS INSTRUCT: the party's best-fighting companions ADD to the kept-XP percent on top of the XP officer's band — each judged by the BEST of the six weapon skills (One/Two Handed, Polearm, Bow, Crossbow, Throwing; movement and command skills deliberately excluded — an instructor teaches arms, not marching), contributing linearly 0 at skill 0 up to XpInstructorBonusPercentAt300 points at 300 (default 5), the top XpInstructorMaxCount fighters counted (default 3 — so up to +15% at defaults), the player excluded (the leader drills, not lectures), the 200% MaxKeepPercent cap still ruling the total. Pure summation in AftermathMath.InstructorBonusPercent; Officers.Instructors picks WHO (sorted best-first, cut to count, each an Officer with role "Instructor" and the weapon that made the grade as skill name); EffectiveXpKeptPercent grew the instructors out-param, the bonus rounded ONCE (InstructorBonusPercent helper) so muster note and kept rate always agree; the muster line and last_drill_report.txt both name the instructors ("instructors +7%: Rolf (Two Handed 220), Borcha (Bow 180)"). Config v13 (new keys, safe defaults, nothing to migrate), MCM: two sliders in the Quartermaster group + the Steward divisor beside the cooldown in Pacing & costs (orders renumbered), McmBridge synced both ways. 12 new Core tests (divisor lerp/clamps + instructor sums/clamps/empties) — 103 green. Awaiting playtest alongside the castle/sea batch. (2026.07.26 08.55.33)
- [x] CASTLE DEFENSE HARDENING + THE ORANGE WALLS (Anton's morning report: "castles worked ok when I chose to attack but totally crashed during defenses" — the ledger showed his 21:32 defend crash IS crash round 2, whose camp-swap fix landed at 21:40, eight minutes AFTER he stopped: the repo's defend road was newer than anything he'd played and had never run once). So instead of a re-fix, a full AUDIT of the swapped shape against the decompiled corpus — verified sound: AddInsideSettlementParties auto-joins garrison/militia to the defense and EXCLUDES the main party (so ApplyInternal's InterruptEncounter menu can never fire mid-launch), Town.GetDefenderParties excludes caravans/villagers (our snapshot exclusion matches vanilla exactly), CheckNearbyPartiesToJoinPlayerMapEvent early-outs for IsSiegeAssault (no passing lord can wander into a siege drill), and JoinBattle(Defender) reads EncounteredBattle = the visit encounter's settlement MapEvent — precisely our shape. THREE real holes closed: (1) founding teleport — StartSiegeEvent(castle, main) moves the main party to the besieger-camp spot outside the walls (OnPartyJoinedSiegeInternal sets Position) while the defender never left the castle; position snapshotted and restored around the swap; (2) port-castle blockade — the SiegeEvent ctor ACTIVATES a blockade when the settlement HasPort and the founding besieger owns ships (the player's own fleet at anchor!), leaving the swapped-in shipless temp party "holding" one; DeactivateDrillBlockade (reflection to the internal DeactivateBlockade) stands it down on both roads; (3) teardown menu push — SiegeEvent.FinalizeSiegeEvent calls GameMenu.SwitchToMenu("siege_attacker_left") whenever the player sits inside the besieged settlement (defend road: always), and at a menu-less moment that throw would abandon the rest of the teardown, leaving a ghost siege in the save; both dismantlers now call the SETTLEMENT's idempotent FinalizeSiegeEvent() first (nulls Settlement.SiegeEvent so vanilla's menu branch can't match) before finalizing the event. LOGGING: every siege step now writes to training_battles.log — founding, blockade state, swap result (camp leader/faction, main freed), defenders auto-joined count, which join branch ran, seated/already counts, dismantle + wall-restore outcomes, aftermath/abort entry state, castle re-entry — a round-3 failure names its exact line. THE ORANGE WALLS (Anton's second ask: the training force must wear the orange-and-white-cross): the siege wall team wore the CASTLE OWNER's colors — the player's own — because mission team color/banner come from the side LEADER combatant and a settlement's PrimaryColorPair is its map faction's pair (verified MissionCombatantsLogic.AddEnemyTeam → PartyBase.PrimaryColorPair → Mission.SpawnTroop ClothingColor1/2 = team colors at spawn); no clan dressing can reach it. New static PaintEnemyMissionTeams writes the training banner's colors + banner onto every enemy team's private backing fields (<Color>k__BackingField etc. — Team has no setters, reflection, no Harmony) from the new SubModule.OnMissionBehaviorInitialize override — which runs AFTER MissionCombatantsLogic built the teams and BEFORE any agent spawns (verified in Mission.AfterStart). No-op unless TrainingActive and UseOpponentBanner; covers field/sea/siege alike (field and sea were already orange via the dressed lender clan — now doubly guaranteed). Build clean, 103 tests green, deployed. Awaiting siege-defense playtest round 3. (2026.07.26 09.28.20)
- [x] SIEGE AUTO-RESOLVE + ROUND-3 VERDICTS (Anton's round-3 playtest, 2026.07.26 morning: TWO clean castle DEFENSE battles at Tirby — the camp-swap defend road WORKS, every step in the ledger). His follow-ups, all settled same morning: (1) AUTO-RESOLVE FOR SIEGES — the old "no hill to watch a siege from" block fell to a decompile find: vanilla's own ordered assault (MenuHelper.EncounterOrderAttack) is the SAME GameMenu.ExitToLast + PlayerEncounter.InitSimulation + mapState.StartBattleSimulation road the field send-troops drill already rides — no siege strategy machinery needed, the walls' advantage lives in the simulation models. LaunchSiegeBattle now takes `simulate` and forks after the sides are seated (both roads share the whole encounter build). The catches caught in advance: BattleSimulation's ctor arms PlayerSiege ITSELF (isSimulation: true) for siege assaults, so the attack road's own StartPlayerSiege stands down when simulating; the engineer's engines are MISSION data and sit out a sim — the send road never charges their bill (the send option got its own TB_SEND_COST_SUFFIX label variable since the shared one would show the begin price; bench picks stay armed for a later led assault, the tooltip says so); and tick trigger (b) ("player bailed to a foreign menu") now stands down while encounter.BattleSimulation != null — the castle sim runs with the settlement menu context alive UNDERNEATH the simulation view (the field sim exits to the bare map), and without the guard the drill would have been finalized mid-simulation. Break In from the sim rides vanilla's mission opener: no bench engines (campaign engine containers empty by design) but the orange team paint still applies. (2) ORANGE MAP LOOTERS during the drill window judged BY DESIGN and kept: shield heraldry comes from PartyGroupAgentOrigin.Banner = the map FACTION's banner for leaderless parties (CustomBanner is NOT consulted — decompile-verified), so the lender clan must wear the colors while the mission runs; what Anton saw "become normal after clicking menus" was the map icons' lazy visual rebuild catching up to the aftermath's restore. (3) NUMBERS VERIFIED row by row (his "check the logs just in case"): battle 1 kia 126 → 7 truly died (5.6% vs 2.55% — unlucky binomial, not a bug), battle 2 kia 80 → died 2 / wounded 20 / downed 104, every surgeon band on its configured chance, the XP clamp-restore (+1184) exact per stack; "(0 xp kept)" lines are stacks whose clamp losses ate the visible earnings — the documented conservative design. The drill report also gained a "— the company —" header so the main pass reads apart from the garrison/militia sections. Build clean, 103 tests green, deployed. (2026.07.26 09.58.00)
- [x] CASTLE MOCK ENEMY — the phantom army comes to the walls (Anton's three calls, settled in-session: garrison and militia ALWAYS defend and the phantoms reinforce them when the player storms / besiege when the player holds; the player's OWN engineer builds every engine and the player pays the bill; same castle rates but the phantoms invisible to wages and prestige — "like they are not there", MCM's EnableMockEnemyTraining the immersion valve). THE FIND that made it a half-session: nearly everything already composed — the mock option sits on the SHARED muster menu with no siege gate (it showed at castles all along), LaunchTrainingCore raises the phantom party before the siege fork, LaunchSiegeBattle takes ANY opponent party, and the leaderless camp-swap, the attack road's EnterSettlementAction seat, SeatFriendliesOnTheWalls, the empty-opponent-snapshot aftermath, phantom prisoner sweeps, stale recovery (mock prefix already in RecoverStaleDrillSieges) and even the Begin/summary labels ("you hold the walls against the mock enemy") were all pre-wired for both shapes; ComputeTrainingCost and CastleDrillRenown count friendly men only, so call 3 held for free. THE ONE REAL FIX — the death book: HarvestBattleDead summed DiedInBattle over EVERY party on BOTH sides, phantoms included, so a mock enemy sharing a troop TYPE with the player's men (or the garrison — near-certain when the mock wears the realm's culture) inflated that type's KIA docket: clamped to the roster hole, but a stack whose men were all merely downed would see phantom corpses fill the clamp and push every downed man into the real-death band. LATENT FIELD-MOCK BUG since the mock mode landed — any "mock drills kill too many" impression predating this is explained. The harvest now skips the mock party's book (guarded on _opponentIsMockEnemy + party identity, both still live at harvest time); split drills still sum both sides, that dead book is all ours. Plus a siege-aware mock tooltip (TB_tip_mock_siege) naming which side the phantoms take on each road. 103 tests green. Playtest points in AI_NOTES (same-culture mock at a castle is the sharp one: last_drill_report.txt eventDead must not count phantom corpses). (2026.07.26 15.52.10)
- [x] THE SPAM CUT + the (?) window (Anton's ask, 2026.07.26: the muster's drill paragraph had grown into a wall since the officers/castle updates). DrillSetupLine is one GLANCE line again — real-death %, XP kept %, cost in denars, disorganized, cooldown hours (Steward-discounted via new ShortCooldownNote, no officer named) — ending in The (?) below reckons it in full.; the real-death chance stays on the line by Anton's hard announce-in-the-muster rule. All the arithmetic moved to a new 	raining_reckoning menu option How the numbers are reckoned (?) (after training_ground, LeaveType.Conversation, shown when a drill mode is on): its click opens a plain InformationManager.ShowInquiry window (The officers' reckoning, pauseGameActiveState, \n\n-joined paragraphs — OfficersReckoningText) that reuses the untouched full note builders: CasualtyNote (three surgeon bands + name) + Heroes never truly die., XpKeptNote (XP officer + instructors), the bill's parts (days wages, castle/sea rates, engine share), castle renown/influence + the garrison-always-defends note, the named CooldownNote, disorganized. The siege head paragraph also shrank to one line (the take-garrison-men-first advice already lives in the castle door tooltip). InquiryData ctor verified against decompiled AllianceCampaignBehavior (title, text, affShown, negShown, affText, negText, affAction, negAction). Build clean, 103 tests green. (2026.07.26 16.29.23)
- [x] THE SIEGE AUTO-RESOLVE WAS A FIELD BATTLE IN DISGUISE (Anton's attack-road playtest, 2026.07.26 afternoon — his one-line diagnosis `not auto resolve on a siege but on a rally out` was exactly right, one enum over: SiegeOutside, not SallyOut). The decompile-verified chain: StartBattle's inside-parties sweep (MapEvent.AddInsideSettlementParties) WALKS OUT any inside party that cannot legally join the defense — SiegeEvent.CanPartyJoinSide fails for the bandit-faction temp party, LeaveSettlementAction ejects it (the `1 guy outside swinging` on Anton's map; the CLAUDE.md risk note about guesting lords being pushed out was this same sweep, aimed at us all along) — and our late defender seat then added an OUTSIDE mobile party, which MapEvent.AddInvolvedPartyInternal answers by silently flipping _mapEventType Siege→SiegeOutside. SiegeOutside's SimulationContext falls back to the map position: NO wall power in GetTroopPower, different run-away semantics, and the winner/loot books ran on the wrong type (a Victory banner over a battle his side visibly lost, `You earned 100.0% of the loot`). The LED assault never noticed through two clean round-2 playtests because the drill opens the siege mission ITSELF — only the simulation reads the event type. Second staging sin found on the way: the sim ran over an UNPREPARED camp (`Building Siege Camp` HUD) — vanilla only ever orders an assault from a camp with SiegePreparations complete. THREE FIXES: (1) re-enter the temp party through the gate after the sweep and before the seat (a second EnterSettlementAction — CurrentSettlement != null at seat time means line 643 never fires); (2) EnsureSiegeAssaultType belt after seating on BOTH roads — restores _mapEventType to Siege by reflection with a loud `EVENT TYPE DEGRADED` ledger line if any other road ever degrades it; (3) PrepareDrillCamp on both roads — SiegePreparations.SetProgress(1f), a public setter, so the HUD and player-siege machinery stand in vanilla's ordered-assault state. Also noted: the ledger shows NO aftermath after the sim — Anton quit at the result panel; with the type fixed the Done → encounter-menu → trigger-(b) chain should finalize, flagged for the re-test. 103 tests green. (2026.07.26 16.47.32)
- [x] THE POST-SIM ZOMBIE MENU SHOT (Anton's round-5 attack-road auto-resolve, 2026.07.26 evening: the round-4 fixes made the sim itself run right — then Victory handed him vanilla's re-startable `encounter` menu: Attack refought him ALONE against the walls, Retreat ran a REAL capture pass and minted the garrison's wounded as his prisoners, Leave dropped him in the castle). ROOT, decompile-verified: `PlayerEncounter.BattleSimulation` is nulled ONLY inside `PlayerEncounter.Finish` — after Done/Retreat the sim object still hangs on the encounter, so trigger (b)'s `BattleSimulation == null` guard (added in round 3 to keep the castle sim from being finalized mid-simulation by the settlement menu underneath) muzzled the drill's finalize FOREVER on EVERY sim road — the field send-troops road included, since the guard postdates the field-sim playtests. FIX 1 — trigger (b2): when the sim's result panel is closed (`IsSimulationFinished` covers Done, `IsPlayerRetreated` covers Retreat, which ends the panel with no winner) and the active menu is vanilla's `encounter`, finalize immediately — the zombie menu lives at most one tick; a LIVE sim still stands the old guard. FIX 2 — aftermath 1b-final, the LAST prisoner net: any non-hero prisoner delta the drill added that the earlier sweeps didn't claim (own types walk home in 1b, mock phantoms dissolve in 1b-mock) is REMOVED with a ledger line each — the garrison-capture leak was minting free prisoners precisely because RestoreFriendlyParties restores those same men onto the walls. Anton's save from the broken round carries the minted prisoners — dismiss by hand or reload. 103 tests green. (2026.07.26 17.24.15)
- [x] THE STARVED SCENE POOL — the ground choice that was never there (Anton, 2026.07.27: attacking bandits he saw no "Select the battlefield" option at all, and "Ride out and scout a battlefield" put him straight into a field without ever asking which). ONE cause wearing two faces: the candidate pool was exactly 1 — `SurveyGroundCondition` bails at `Count < 2` (nothing to choose between) and `ScoutGround` skips its picker at `Count == 1`. THE DATA TRUTH, read out of both shipped `sp_battle_scenes.xml` (SandBox + NavalDLC): every LAND battle scene in this game is tagged Plain (73), Steppe (30), Desert (14) or Swamp (7) — no other terrain owns a battlefield, while `TerrainType` has 24 members (Forest, Mountain, Snow, Canyon, Bridge, Fording, RuralArea, Beach, Cliff, Lake, Water, Dune…). So the "wider same-terrain pool" that CLAUDE.md credits with keeping the pickers alive matched ZERO scenes anywhere off the four blessed terrains, and with each of the 154 claimed map patches owned by exactly one scene (re-verified this session) the pool collapsed to that one patch scene. Bandits camp in woods and hills — which is why Anton hit it there and never on the plains, and why the bug outlived every playtest of the feature. THE FIX — `WiderPoolAt` is now TIERED, nearest ground first: (1) the patch's own scenes (`localCount`, "this ground"); (2) the local terrain type (vanilla's own fallback, untouched); (3) every scene whose `<TerrainTypes>` block LISTS the local terrain — the data's secondary-feature list, read by neither us nor vanilla until now (Mountain rides in 14 scenes, Water 7, River 7, Canyon 2, Lake 1, Dune 1), so a Plain field with a mountain in it answers a party in the mountains; tiers 1-3 are `nativeCount`, the country truly stood in; (4) the KIN terrains (new `KinTerrains` table: Forest→Plain/Swamp/Steppe with the block sorted by ForestDensity descending so the thickest fields come first, Snow→Plain/Steppe, Mountain→Steppe/Plain/Desert, Canyon and Dune→Desert, every bridge/ford/river shape→River/UnderBridge/Plain/Swamp, Lake→River/CoastalSea, Water→CoastalSea/OpenSea, CoastalSea↔OpenSea); (5) still under two entries — the whole board, vanilla's own last-ditch shape. The naval-ness filter runs on every tier, which is why ONE kin table can serve land and sea: a sea pool can never take a field, nor a field pool a sea. Honesty kept: everything past `nativeCount` is labelled "— another country" in the shared picker, so fighting a Battanian forest skirmish on a Vlandian plain is the player's informed pick and never a silent swap; `Describe` already names each scene's terrain beside it. `WiderPoolAt` keeps its old 3-arg overload (the conditions and the muster's battlefield line don't care), the new 4-arg one adds `nativeCount`; `ShowPicker` takes it as a required param and all four doors pass it. Diagnosis aid for the next report: a `[ground] scene pool: land | terrain Forest | patch 1 | native 1 | pool 58` line in training_battles.log, deduped like the duel line (menu conditions call the pool every frame). Build clean, 103 tests green; DEPLOY BLOCKED — the game was running and held the DLL, Anton runs `tools\deploy.ps1` himself. (2026.07.27 13.10.41)

- [x] THE TEAMLESS SCOUT RIDE — a null the whole modding ecosystem could trip on (froggyluv's Nexus report 2026.07.26 "CTD when you ask to Scout the Battle area first" with War Cry / War Horns enabled; his stack trace, posted 2026.07.27, named the spot exactly: `WarCryWarHornOrderHorns.CheckAndInitializeCaptainsOnceWarCry` line 306, called every frame from their `WarCryMissionBehavior.OnMissionTick` line 903, under `Mission.OnTick`). The crash is in THEIR code, but the hole was OURS: `ScoutMission` opened a battlefield with no battle and never created a single Team, so `Mission.PlayerTeam`, `Mission.PlayerEnemyTeam` and the lone rider's `agent.Team` were all null — and their captain scan, like plenty of mission mods, assumes every mission has sides. No vanilla mission is that shape: even the empty village/camp walk-arounds run SandBox's `MissionBasicTeamLogic` (Defender + Attacker + a third ally team, colors from the map faction, PlayerTeam set) — decompile-checked this session, along with `TeamCollection.Add(side, color, color2, banner, isPlayerGeneral, isPlayerSergeant, isSettingRelations)`. THE FIX, `ScoutMissionLogic.EarlyStart` (mirrors MissionBasicTeamLogic but honors the scouted side): one Defender team, one Attacker team — the player's side wearing `Hero.MainHero.MapFaction`'s color pair, the empty side white — `Mission.PlayerTeam` set to the side being previewed, and the rider spawned INTO it via `AgentBuildData.Team`; guarded by `Teams.Count > 0` so any behavior that gets there first keeps its own teams. Verified harmless to the ride: `Mission.IsFriendlyMission` is a plain public field that only battle logics clear, so Mission.OnTick's hold-Tab leave branch is untouched (Anton playtested the ride same day: works fine), no combat/spawn/end logic rides along, and both teams stay empty of agents. The SEA scout never had the hole — it is built on the naval custom-battle recipe, whose SetupTeams makes real teams. NOT reproducible here (War Horns isn't installed, its source is the author's private repo): if the CTD survives, ask for a fresh trace — a scan that walks `FormationsIncludingEmpty` and reads `formation.Captain` unguarded would still fall over on empty formations, and only they can null-check that. New CLAUDE.md footgun line: a teamless mission is a mod-compatibility trap. Shipped in v1.3.2 with the starved-scene-pool fix (Steam item 3770681619, "Uploading done!"). 103 tests green. (2026.07.27 15.42.10)

- [x] Battles NEAR A VILLAGE — the ground tools stop lying, and the scout rides the village itself. Anton's report (2026.07.28, the "Basil Near Village Looter Atk" save): attacking looters near a village, the fight happens IN the village, yet "this ground" and the scout ride both showed a plain field with scattered trees and the village was nowhere in the picker — plus a doubt that we pull the wrong maps at all, because a fight while standing mid-bridge on the campaign map gave a bridgeless field. RESEARCH SETTLED BOTH. (1) The village rule is vanilla's and I had not accounted for it: `MapEvent.Initialize` (CampaignSystem, ~line 776) takes ANY land field battle the main party is in, finds the nearest village inside `EncounterModel.GetSettlementBeingNearFieldBattleRadius` (3.0 map units, `DefaultEncounterModel`), and sets `MapEventSettlement` to it — the event TYPE stays FieldBattle, only that pointer moves (and `MapEvent.Position` jumps to the village). `MenuHelper.EncounterAttackConsequence` then forks on the pointer: a village sends it to `PlayerEncounter.StartVillageBattleMission`, which opens `settlement.LocationComplex.GetScene("village_center", 1)` at scene level `land_raid` and NEVER calls `SceneModel.GetBattleSceneForMapPatch` — so `TrainingBattlesSceneModel.PendingSceneId` is never read and the survey pick was silently discarded, while the scout walked a field the battle would never use. Village battlefields are settlement LOCATIONS, not `SingleplayerBattleScenes` (274 villages over 86 distinct scenes in settlements.xml), which is why `BattleSceneCatalog` could not see them. Consolation find: that path still fills `AtmosphereOnCampaign` from `MapWeatherModel.GetAtmosphereModel`, so the hour pin already worked in village battles. (2) The bridge doubt is NOT us — the game ships no bridge battlefield at all: `sp_battle_scenes.xml` tags every one of its 80 active land scenes Plain/Steppe/Desert/Swamp (57/30/14/7) with zero Bridge or UnderBridge and no `*bridge*` id; `SandBoxCore\SceneObj` holds 98 `battle_terrain_*` folders, none bridge/forest/snow/river; and 154 of the 158 map-patch indices are claimed (orphans 0, 52, 54, 64), each by exactly one scene. Standing on a bridge, the patch belongs to the surrounding region's ordinary field and vanilla loads that same field — Anton's own log agreed (`terrain Plain | patch 1 | native 56 | pool 96`). BUILT (no Harmony, every API public): `BattleSceneCatalog.VillageBattlegroundFor(mapEvent, out village)` is the single truth — field battle + `MapEventSettlement.IsVillage` + not naval (vanilla's sea-raider fork takes the ship road) → the village's scene id; `VillageBattleSceneLevels` holds vanilla's own `"land_raid"`. `RealBattleGroundBehavior` now shows "Select the battlefield" DISABLED there, naming the village, instead of offering a pick the game throws away; and "Ride out and scout a battlefield" rides the village itself with no picker at all — there is nothing to choose between when the village IS the battlefield. `ScoutMission` grew `CreateSettlementRecord` (the patch-aware record minus the patch, plus `SceneLevels`) beside `CreatePatchAwareRecord`, both now one private `CreateRecord`; because a settlement scene carries no map patch, `BattleSpawnPathSelector` picks the deployment path at RANDOM — vanilla's village battle does the same — so the old `realEncounter` bool became `ScoutMission.LinesTruth` (Drill / Exact / GroundOnly) driving three honest report wordings, the village one promising the ground and explicitly not the lines. New log line `[scout] village ride: <scene> (<village>) | side <side>`. The MUSTER is untouched and correct as it stands: our drill builds its own record and never reaches vanilla's village fork, so a drill near a village still fights the map-patch field. DELIBERATELY NOT BUILT, left as Anton's call on the board: pulling a near-village battle back OUT onto a chosen field (both roads exist — `MapEvent.OverrideMapEventSettlementForRaidToFieldBattleSwitch(null)` is a genuine public setter, or our own attack option replicating the field branch — but each touches real campaign consequences that read `MapEventSettlement`), and the village green as a DRILL ground (not free after all: both pickers are typed to the game's `List<SingleplayerBattleSceneData>` and a village is not one, so it needs our own candidate record threaded through `ShowPicker` and both doors). 103 tests green, deployed, and playtested the same day on the reporting save — "works fine" (Anton): greyed option naming the village, scout ride entering it, battle fought on that same ground. (2026.07.28 14.52.00)
- [x] THE MOD DID NOT LOAD WITHOUT WAR SAILS — v1.3.0-v1.3.3 unloadable for every player without the DLC, fixed with the mod's first SATELLITE ASSEMBLY (Nexus report, Darilon 2026.07.28: "1.3 and beyond gets a dependency error on game start. 1.2 runs fine… I don't have War Sails but it's optional according to this mods description" — right on every count). THE MECHANISM, read out of the game's own decompiled loader rather than guessed: `Module.AddSubModule` → `CollectModuleAssemblyTypes` calls `moduleAssembly.GetTypes()`, and a ReflectionTypeLoadException there returns `AssemblyLoader.AssemblyLoadResult.CriticalError` → `HandleSubmoduleLoadError` → startup error dialog, module never loaded. `GetTypes()` LOADS every type, and a type load eagerly resolves its BASE TYPE, its INTERFACES and its FIELD types — while method BODIES are JIT-compiled on first call, which is exactly why the bodies-only soft-dependency rule works and why everything else in SeaScoutMission was safe. THE OFFENDER was the one class that broke the rule: `SeaScoutDeploymentController : NavalDeploymentMissionController`, shipped with the sea scout ride in v1.3.0, whose class doc called it "the one sanctioned break… proven harmless, same shape as the MCM settings class" — it was neither harmless nor proven, and the precedent it leaned on is the same bug (below). THE FIX: `src/TrainingBattles.Naval/` (net472, references NavalDLC + the module; the arrow points ONE way so there is no cycle and no load-time pull) now holds that controller, and the new `NavalBridge` in the module loads the satellite BY HAND — from the module's own bin folder via `Assembly.LoadFrom` (the game's module bin is not the AppDomain probing path), only after confirming NavalDLC is already in the AppDomain, then `Activator.CreateInstance` and back as a plain `MissionBehavior`; one attempt per session, every failure logged, and if the satellite cannot be had the ride falls back to the DLC's own controller constructed in a method body — it still sails, only the flight recorder goes quiet. SubModule.xml never names the satellite (the game loads only the declared DLL), deploy.ps1 and package.ps1 now build the satellite project (the module comes along as its project reference) and copy TrainingBattles.Naval.dll beside the module. THE REAL LESSON is that this could never be caught by playing: on a machine that owns War Sails the mod is perfect. So the session also built `tools/AssemblyGuard` — a hermetic metadata reader (System.Reflection.Metadata, no game DLLs, no install, nothing loaded) that flags every forbidden assembly appearing in a type's base type, interfaces or field types, warns on method signatures (lazy, but some frameworks reflect over them), and handles the generic-base shape (`AttributeGlobalSettings<T>` arrives as a TypeSpecification, not a TypeReference — miss that and the guard blesses the very class it exists to catch). Verified against the shipped zips in dist\: v1.2.0 clean of NavalDLC, v1.3.0 and v1.3.3 both flagged, HEAD clean; package.ps1 now runs it as a release gate (War Sails hard-fails the package, MCM warns). A second harness (MetadataLoadContext with NavalDLC withheld from the resolver) independently confirmed the same before/after. THE SECOND DOOR, FOUND AND LEFT OPEN ON PURPOSE: the guard flags `TrainingBattlesMcmSettings : AttributeGlobalSettings<T>` plus its two `Dropdown<T>` backing fields in EVERY release since v1.0.0 — by the identical mechanism a player without MCM cannot load the mod either, though the manifest and the mod page both call MCM optional. No report has ever come in (MCM is near-universal), the fix is the same satellite shape, and the risk that makes it its own session is that MCM DISCOVERS settings classes by scanning loaded assemblies: load the satellite too late and the settings menu silently vanishes for everyone. Anton's call the same hour: fix it next, in its own session with an MCM-menu playtest — line in TASKS_TODO, design in AI_NOTES. 103 tests green, deployed, and SHIPPED AS v1.3.4: a mod that will not start is the standing exception to the new batching rhythm (below), so this one went out alone, with change notes that name the mistake and thank the reporter. (2026.07.28 20.14.00)
- [x] RELEASE RHYTHM CHANGED (Anton, 2026.07.28): "can we collect fixes throughout the week somehow and not updating the version each day?" — so the version in module\SubModule.xml is now bumped ONLY on release day, not per fix. Landed-but-unreleased work collects in a new `SHIPPING NEXT` section at the top of TASKS_TODO.md (short lines, Anton's board rules unchanged), TASKS_DONE.md keeps taking its dense entry per piece of work as always (it is the changelog of WORK, not of releases), and the release step is now: bump the manifest version once, empty SHIPPING NEXT into the public notes, run package.ps1 (which gates on the soft-dependency guard), upload. CLAUDE.md's workflow section carries the rule so no future session bumps a version out of habit. (2026.07.28 20.14.00)
- [x] THE TOWN UPDATE — the siege drill comes to owned TOWNS, and past the walls the LORD'S HALL (Anton picked the rates and "fight the keep" 2026.08.02; castles landed 2026.07.25, towns ride the same code). THE GATE: the muster door now stands on the "town" menu too (own option id, same shared FortDoorCondition — IsCastle || IsTown, owner-clan, per-settlement clock), towns pay their OWN rates: TownTrainingCostWages default 10 (double the castle's 5 — Anton's old 50× sketch retired) and TownTrainingCooldownHours default 336 (his old two-week instinct, kept), both in config.json + MCM ("Pacing & costs", renumbered; the Features toggle EnableCastleTraining KEEPS ITS KEY NAME for config-compat but now reads "Siege training (castles & towns)"; the renown/influence per-100-men keys are SHARED with castles — a town's bigger field scales them naturally). The per-settlement cooldown string was already settlement-id-keyed, so towns just add entries (save key stays "TrainingBattles_CastleCooldowns" — save-compat). Cancel now returns to the RIGHT settlement menu ("town" vs "castle" — the old code hardcoded castle). THE LORD'S HALL (the town flavor, attack road only): DATA TRUTH from the decompiled corpus — vanilla arms the pull-back rule in EVERY player-attacker siege mission (SandBoxMissions.OpenSiegeMissionWithDeployment: BattleEndLogic.EnableEnemyDefenderPullBack(20), NO IsTown check — the old "castles never enter the hall" belief is FALSE for this game version, so our CASTLE drill could already end "defenders pulled back" and silently settled; now both kinds offer the keep). The machinery: 20+ ROUTED defenders + side depleted → MissionResult.CreateDefenderPushedBack (BattleState.DefenderPullBack → CampaignBattleResult.EnemyPulledBack, BattleResolved FALSE, event winnerless) + CampaignSiegeStateHandler (rides every siege mission incl. ours) flips Settlement.CurrentSiegeState OnTheWalls→InTheLordsHall; vanilla's next screen is the re-attack encounter menu — the EXACT menu our trigger (b) kills — so a new tick catch (a2), placed between (a) and (b), steers to our own two-option GameMenu "training_hall_menu" instead: "Storm the Lord's Hall" (vanilla's MenuHelper lords-hall road copied verbatim: attacker cap = max(1, min(model.MaxAttackerSideTroopCount=19, round(hallDefenders × 0.7))), MenuContext.OpenTroopSelection over the main roster with GetStrongestAndPriorTroops preselected, Done → MapEvent.ResetBattleState() + OpenSiegeLordsHallFightMission(LocationComplex "lordshall" scene at wall level) — the mission BUILDS ITSELF from the live map event via PartyGroupTroopSupplier priority rosters, defender list minted inside the open call, max 27 defenders / 0.7 archer cap) and "Let them stand — end the drill" (FinishTrainingBattle — the pre-hall behavior). GUARDS learned the careful way: (1) HallStageOpen requires PlayerSiege.BesiegedSettlement == the drill settlement — MapEventSide.forcePriorityTroops only applies priority rosters under a live player siege, without it the hall would spawn EVERYONE (the attack road arms StartPlayerSiege already; if it ever failed its try/catch, the offer stands down and the drill settles as before); (2) CampaignBattleResult.BattleResolved gates the door — the settlement stays InTheLordsHall until teardown resets it (Settlement.FinalizeSiegeEvent → ResetSiegeState, which BOTH our dismantlers already call), so after a DECISIVE hall fight only the result tells the door to stay shut; a hall RETREAT (unresolved) re-offers the storm, vanilla-style; (3) the catch idles while the player sits at the hall menu or its selection popup (no re-switch spam), waits bounded (300 ticks) for vanilla's menu when the map is bare, and every failure road inside the hall launch settles the drill instead of hanging it. The summary knows the keep: a fought hall forces the "walls carried" sentence (the hall only opens after a player walls win) + "The Lord's Hall fell with them." / "The Lord's Hall held." Aftermath, surgeon bands, XP officer, prestige, mock phantoms (they reinforce the garrison on the attack road and can man the hall — their dead stay off the books via the existing HarvestBattleDead skip) all unchanged — the hall casualties land in the same map event the aftermath already harvests. Build clean, 103 tests green. NOT YET PLAYTESTED — eyes on: the hall mission's spawn caps with a mock-reinforced garrison, the pulled-back castle drill (newly offered keep), a hall retreat → re-storm loop, and the town door's index-4 seat on the crowded town menu. (2026.08.02 11.45.20)
- [x] TOWN PLAYTEST ROUND 1 answered same day (Anton, 2026.08.02: "both sides are collored with mine banner" + "send men in 79k, carry out the atk 82k - siege eq might not be calculated if auto"). FINDING 1 = REAL BUG, root-caused in the corpus and fixed: every big banner surface reads the MAP EVENT SIDE LEADER - the led scoreboard (SandboxMissionBattleScoreContext) and the auto-resolve panel (SandboxSimulationBattleScoreContext) read side LeaderParty.Banner, and MissionCombatantsLogic births each team from the side-leader combatant's banner + MAP-FACTION color pair - and on the attack road the enemy side is LED BY THE SETTLEMENT, so a drill on your own walls named YOU the enemy (castle DEFENDS always looked right only because the enemy leader there is the orange-dressed temp party; the team paint itself WAS landing - the [colors] log lines proved it - but it only feeds uniform tint, which spawn reads from agentTeam.Color). Shields were a second, separate read: leaderless troops' heraldry comes from their party's MAP FACTION banner (PartyGroupAgentOrigin.Banner - its SetBanner literally throws NotImplementedException), so the garrison's and militia's shields CANNOT be recolored without dressing the player's own faction - accepted and documented; the picked half / phantoms already carry orange shields through the lender-clan dressing. THE FIX: Settlement.Banner honors Party.CustomBanner FIRST (verified: !IsMobile -> Settlement.Banner -> Party.CustomBanner), so FlyTrainingBannerOverFort flies the training banner over the settlement party while it leads the enemy side (attack road only, set beside the blockade stand-down so both the led and sim roads get it), and SweepFortTrainingBanners - STATELESS on purpose, no reliance on _siegeSettlement which the exit roads null at different times - walks every player settlement and clears exactly our banner (serialized-code match, another mod's custom banner untouched) at aftermath + abort + session load, because CustomBanner is [SaveableProperty] and a crash must not leave the town flying the orange cross forever. FINDING 2 = BY DESIGN and already labeled: the launch subtracts SiegeEquipmentBill() on the simulate road (an ordered assault fields no bench engines - they are mission data, the sim only knows the walls advantage), the send option prices itself with its own suffix (Anton's 79k) and its tooltip says the engines sit out and are not billed; picks stay armed for a later led assault. No change. Build clean (warning baseline held at 6), 103 tests green. (2026.08.02 13.10.59)
